# Changelog

The single record of what changed. One line per change, newest first.

- Calibration (Richard's call): base landlord NOI 1.2 → 1.6 — the global anchor moves to the MIDPOINT of the bullish case ($1.76M/MW·yr cheap-owned; conservative = WULF/Anthropic year-1 de-escalated ~$1.5M, bull = term-average ~$2.0M, which is the engine-consistent convention since contracted rent never escalates in the model). All landlords re-rate ~+33%: WULF −28%→−5%, APLD +7%→+53%, CIFR +22%→+72%, RIOT +13%→+48%, KEEL +144%→+226%, HUT −64%→−39%. WIKI benchmark line updated.
- WULF: Anthropic DIRECT 20-yr lease (8-K 7/6/26) — ~$19B / ~401MW critical IT at Hawesville KY 'Justified Data' (phased 2H27→early-2028, 2x5yr renewals, IG credit support reportedly Google; Century Aluminum's 6.8% Raylan minority already in seniorClaims). Simultaneously SOLD the 50.1% Abernathy JV stake to Fluidstack for ~$530M (~1.18x invested) — resolves the Abernathy over-credit watch-item; ~$500M receivable booked in legacy. Sites: +401MW disclosed Anthropic rows, −168MW Abernathy; contracted 38→45%; term 13→16yr. Economics verdict: implied YEAR-1 NOI ~$1.5-1.6M/MW (term-avg ~$2.0M incl. 2.5-3% escalators) — top of the market cluster (~$1.3M), at/above WULF's own prior Hawesville estimate, still ~35-40% below premium NoVA (DLR $2.51M). Validates the rising-rent trend (+3%/yr dial ≈ the signed escalators) and the lease-up conviction (empty smelter bought Feb-2026, 401MW leased within 5 months). Target $17.40→$22.21; new watch-item: KY permanent financing to term out the $500M MS bridge (→ committedDebt when issued).
- Portfolio: BTBT EXCLUDED from the paper book by mandate (Richard 2026-07-06: special case — holdco/SOTP discount vehicle; stays in the tracker for the HUT/WYFI look-throughs). New `exclude` mechanism in portfolio.json (ticker → basis, checks-enforced like every judgement input): excluded names are uninvestable for book AND benchmark — the position sells at market at the next mark, the benchmark rebuilds immediately (book and yardstick must share a universe), and the backtest preserves+honors exclusions across regenerations; spec §6b + WIKI updated. Portfolio tab: chart now plots cumulative % RETURN (both lines rebased to 0% at window start — the old axis was NAV index levels) with a 1D / 5D / 1M / 6M / 1Y range selector, plus an ABSOLUTE RETURNS table (portfolio vs equal-weight per window); excluded names badge "out by mandate" in holdings; assets cache-stamped ?v=4.
- Portfolio hardening pass 2 (a second 37-agent adversarial review of the pass-1 fixes themselves: 16 confirmed, 1 refuted). CRITICAL: the freeze carve-out never renormalized target weights over the tradable sleeve — one missed quote on a 58%-of-NAV name would have force-sold every healthy position into cash for a day; targets now renormalize so a freeze at unchanged prices produces zero drift and zero trades. ALSO: corp-action accept band narrowed ±40%→±12% (3:2/4:3/5:4 splits slipped through unadjusted — a silent −19% NAV error on an IREN 3:2), split lookback window now spans the actual ledger gap (was hardcoded 1mo), ledger price history back-adjusts on splits (raw cross-split ratios would have poisoned learning windows as ±50-900% phantom returns), Yahoo-unreachable is now distinct from Yahoo-disagrees (a real crash during a Yahoo outage freezes WITHOUT quarantine credit instead of blacking out the ledger on day 3), quarantine escalation now records the day's mark and commits BEFORE turning the run red (PORTFOLIO_ALERT plumbing), the benchmark monthly rebuild carries frozen names instead of liquidating halts at phantom prices and defers on trade-suspended days, freshness stamps only after the corp-action screen (quarantined names age into the stale route), suspect names drop out of the investable rows, learning cadence anchors to the last successful learn with era-gated attempts surfaced as ledger notes (first live learn ~day 64, not an accident of backtest length), stale opposition counters reset for names absent from a window, corrupt books abort rebalancing instead of clamping cash to zero, basis validation never rescales off the stale manual fallback price, genesis freshness seeds from actual final-day prints, attribution shows all names (the top-12 slice hid the biggest detractor), a failed background refresh no longer wipes a healthy Portfolio tab, the Checks↔portfolio fetch loop on load failure is broken, the px-vs-fundamentals tripwire tightened to ±35%, and all assets carry cache-busting version stamps (?v=3 — stale browser caches were serving old app builds). Genesis regenerated: NAV 785.7 vs equal-weight 264.9.
- Portfolio hardening (overnight adversarial review: 100 agents, 42 confirmed findings, 5 refuted; all criticals fixed). CRITICAL FIXES: (1) corporate-action guard — a >40% overnight move is now split-checked via Yahoo events (share counts adjust value-preservingly) or quarantined on source conflict (3 suspect days → red Action; a reverse split would previously have booked a permanent phantom NAV gain and phantom trades); (2) ABTC basis break — Yahoo's chart serves ABTC on a 15× different share basis than its own live quote; the backtest now BASIS-VALIDATES every series against a live quote and rescales (return-preserving; ABTC ×0.0667) — without this, Monday's first live mark would have printed a phantom −93% ABTC move into the benchmark forever; (3) learning rule — excess is now measured vs the universe MEDIAN (the arithmetic mean let one +798% moonshot mark +149% winners as 'market disagrees'), m shrinks only on two CONSECUTIVE opposed windows (overlapping 63d windows triple-counted single episodes), recovers only when vindicated, and an era gate keeps simulated-genesis views out of live λ/m; (4) BTC/ETH now stored per ledger record and forward-filled on Coinbase outage (was: silent reprice of ~$4B of treasuries at the static $60k fallback). ALSO: feed-death detection (dead quotes ≠ holiday — Action goes red + opens a GitHub issue), partial-tape days mark NAV but suspend trading, >10d-stale names freeze as uninvestable, removed-from-universe names freeze rather than sell at phantom prices, intraday workflow_dispatch refused before 16:05 ET, cost-aware sizing (cash never negative; was negative 189/256 days), tie-averaged Spearman, sub-$10 ledger prices keep 4dp (cents-rounding was a 1.5% error on NUAI), backtest refuses to overwrite live records without --force and fails loudly on missing tickers, git push rebase+retry in the Action, atomic file writes. Spec §6b synced to the implemented weights rule (gross = min(1, Σν⁺/0.75), cash = remainder — not cash-in-softmax). NEW: 'Portfolio ledger' checks group (NAV recompute from holdings, px-vs-fundamentals basis tripwire, date monotonicity, staleness, state bounds — 68 assertions) in checks.js + the Checks tab; Portfolio tab gains per-name ATTRIBUTION (NAV points, simulated vs live columns), operational-notes feed (splits, quarantines, freezes — surfaced, never silent), dials-moved warning on Target-now, auto-refresh, retry on failed load. Genesis regenerated: NAV 750.6 vs equal-weight 264.9 (ABTC investable at the correct basis; sane learning), zero negative-cash days.
- New screen: PORTFOLIO — the self-balancing paper book (spec §6b, new; §2/§6/§10 amended). Sizing rule: ν = λ × confidence × m × ln(target ÷ price); softmax at T=0.15 (concentrated by mandate, no position cap); confidence = (contracted floor + legacy) share of EV less open watch-items; gross exposure scales with total edge (cash grows as it thins); 2pt rebalance band, 5bps paper cost; long-only + cash (shorting reserved). Learning adapts SIZING only, never data.json: λ follows realized rank-IC every 21 trading days, per-name multipliers shrink where the market persistently disagrees (surfaced on the tab; `conviction: true` exempts). Valuation engine extracted to shared `engine.js` (browser + node, one math, as-of date overridable — portfolio targets roll forward daily). 1-yr simulated genesis, 256 trading days (Yahoo adjusted closes + Coinbase BTC/ETH): NAV 675 vs equal-weight benchmark 265 (base 100), max DD 43% vs 54%, peak single-name weight IREN 68% — labeled machinery-validation, NOT alpha (today's inputs contain the year); live state restarts neutral (λ 0.75, all m = 1). Daily mark: GitHub Action 22:35 UTC Mon–Fri → `portfolio-run.js` (Finnhub quotes, holiday/idempotency guards) commits `portfolio.json` + `portfolio-history.json` (the one sanctioned time-series ledger) and Render redeploys.
- APLD: capped calls CONFIRMED (indenture-level research, Nov-2024 8-K Ex 10.2) — $51.75M cost, strike $9.752 / cap $14.72, covering the full 46.1M convert shares; the stock tripled through the cap so the hedge is frozen at ~$229M intrinsic (offsets only ~7M shares at $33, and never reduces what converting holders receive). Added as a $229M legacy asset → target $39.57→$40.13 (+21%). Warrant audit: NO tranche (Macquarie/CoreWeave/Jane Street/MAM/STB/AI Bridge) carries any issuer redemption or buyback right; convert callable Dec-2027+ but a call only accelerates conversion (cash settlement = the sole avoidance, ~$1.5B at $33); Series E preferred (~2.5M sh) is the only cash-redeemable instrument. Full-issuance count 386M validates our ~380M FD (Northland's 285M = basic only).
- Calibration: base landlord NOI constant 1.1 → 1.2 ($/MW·yr), anchored to SIGNED hyperscaler NNN lease prints across five names (APLD $36B/15yr/1.41GW ⇒ $1.34M/MW; WULF Hawesville ~$1.4-1.6M; BTDR Tydal ~$1.1-1.4M; CLSK Sandersville ~$1.0-1.2M; KEEL Panther Creek ~$1.0-1.1M — cluster ~$1.3M vs our prior $1.21M cheap-owned anchor). Cheap-market owned now prices at $1.32M/MW, matching APLD's own signed economics. All landlords lift ~9-13%: APLD +6%→+20%, CIFR →+54%, KEEL →+144%, HUT −64%→−56%.
- Universe → 21: added NUAI (New Era Energy & Digital — single-project TCDC/Odessa landlord option; 650MW secured BTM gas, top-4 hyperscaler in advanced talks, hard Oct-8-2026 Macquarie lease deadline; sites scaled to NUAI's ~20% JV economics under the Apollo/Stream LOI; 20% governance discount → +12%) and KEEL (Keel Infrastructure — CORRECTION: our earlier batch research wrongly dropped the ticker as invalid; KEEL is the Bitfarms successor via 1:1 April-2026 redomiciliation, which pre-April databases masked. Three owned/zoned/power-secured campuses — Panther Creek 350MW PJM, Sharon 110MW, Moses Lake 18MW — ~$920M liquidity, both capped-call converts OTM (stay in net debt per convention), zero leases signed, three targeted YE26 → +124%). Filing-verified capital structures; verified stamps seeded.
- New screen: CHECKS tab — the data test suite runs LIVE in the browser on every page load (shared `checks-core.js`; identical code to the `node checks.js` CLI, 2,400+ assertions). Shows: verdict strip, the 8 check groups with what-each-guards + pass counts, all findings, a per-company ✓/⚠/✗ matrix (hover for messages), filing-verification ages per name (`verified.capital`/`verified.contracts` + `config.verifiedPricing`, colored ≤30d/31-60d/>60d — stamped by the weekly sweep on approval), and the open `watchItems` registry (WULF Abernathy 50.1% JV, CIFR 70-vs-150MW campus, APLD Macquarie draws, MARA AI-MW, CRWV supply-entrant tripwire). A pass/warn/fail badge sits on the tab on every page. Spec §6 → five screens; weekly sweep now stamps verification dates.
- Methodology census tier 2 applied — committed project debt & senior claims, verified to filings: HUT committedDebt $7.5B (River Bend $3.25B + Beacon Point $4.25B closed SPV bonds funding credited campuses) → −64%; CIFR $4,341M ($3,531M escrowed construction SSN proceeds + $810M Stingray closed 6/15/26) + $26M Colchis NCI → +37%; APLD $3,740M (ComputeCo-2/-3 closed post-quarter, funded paper only) + $1,100M seniorClaims (Macquarie preferred ~$970M accreted + ~$130M JV NCI) → +10%; WULF committedDebt $0 (the $3.2B Lake Mariner bond is ALREADY inside its netDebt — escrowed proceeds are the netted cash) + $101M Hawesville NCI; Abernathy 50.1%-JV over-credit flagged for the weekly sweep. Financing is now charged symmetrically with the credit across the board.
- Methodology census applied (tiers 1+3 of 3; 34-agent adversarial review verdict: 'fit for purpose after repairs'). CONSISTENCY FIXES: contract premium & cap compression are now site-aware (rumored MW earns neither — removes ~$19-20B of unearned EV from CRWV/NBIS); ramp applies only to the uncontracted share (take-or-pay leases bill from commencement — un-flips CORZ); landlord cap floored at 6.5% (the DLR print); WYFI rerouted owner→landlord (~97% colo by economics; GPU cloud → legacyEV) + its ITM convert if-converted (shares 41.6→50.5M, netDebt −63) — flips WYFI to overvalued and collapses BTBT +204%→~+29%; BTBT stake diluted-through (pct 0.577) and controlling stakes use pre-discount equity. NEW DIALS (the silent bets made visible): 'Lease-up / spot realization' (base 1.0 — the deliberate scarcity conviction that energized capacity gets rented; 0.42 = consensus spread) and 'Rumored-pipeline credit' (stress the far pipeline without editing data); trend capped at disc−2 so the time axis can't invert; 7× multiple derivation + margin-as-steady-state-EBITDA documented in WIKI. New fields committedDebt/seniorClaims wired into equity (basis required, checks.js enforces); figures for HUT/CIFR/WULF/APLD pending filing verification (tier 2).
- Audit infrastructure: added `checks.js` — a deterministic data test suite (schema, site schedules/phasing, provenance consistency, capital-structure sanity, basis notes on judgement inputs, stake integrity, freshness); runs in seconds, exits non-zero on FAIL; wired into the weekly scheduled sweep (which also verifies FD shares/issuance/contracts/pricing against filings). First run caught 3 FAILs (undocumented equityDiscounts on SLNH/HUT/FRMI — bases added) + 24 legitimate watch-items. Synced stale spec §3/§4/§6 (removed slide-out panel, score, renewalProb, cost-of-debt-floor references); removed dead score code/CSS from app.js/styles.css; WIKI updated. [skip render]
- Added WIKI.md — a terse, present-tense, always-current plain-English reference (formula, dashboard gauge, dials, field glossary, conventions, how to keep it updated, known limitations). Companion to the binding spec.
- Dashboard graphics redesign: replaced the EV-composition stack + split bar + relative-strength score with a single per-row VALUE GAUGE — bar = our target value (segmented contracted-floor / expected-pipeline / legacy-treasury), a dark line = today's market price, and the shaded space between = upside (green) or overvalued (red), scaled per row. One visual now shows the price-vs-value gap, its direction, AND the quality of the value backing it (solid = contracted, hatched = speculative, gold = legacy/stake) — and it works for holdcos. Dropped the opaque relative-strength score from the dashboard and full page.
- Capital-structure overhaul (audited to 10-Q/10-K across the universe, 2026-06-30, prompted by a model-vs-BTC-Miner-Terminal audit + the Northland DLR/Blackstone note): corrected fully-diluted shares (if-converted/treasury — deep-ITM converts + RSUs + penny/ITM warrants, with each counted convert's principal removed from net debt) and net debt for 17 names. Biggest fixes: HUT net debt $7.5B→$85M (the $7.5B was a forward/undrawn SPV project-debt placeholder, not parent debt) → −67%→−27%; CORZ +63%→−2% (+138M shares + post-May refi debt); CIFR +95%→+42%, APLD +73%→+34%, BTDR +79%→+47%, CLSK +37%→+22% (missing convert/RSU dilution); NBIS → net cash −$9.3B if-converted (−3%); WULF −20%→−36%. We were too low on shares for 13/17 names. Spec §5a now codifies the FD-share / net-debt discipline (project/SPV debt netted only to the extent drawn; escrowed construction cash credited).
- Dashboard: the per-ticker thesis is now COLLAPSED by default behind a "▸ why under / over-valued" toggle on each row (expands inline on click, doesn't trigger navigation) — declutters the comparison view. Also fixed the thesis text colour (was referencing an undefined CSS var).
- Dashboard: per-ticker THESIS line — a 2-3 sentence, high-signal read on why each name is under/over-valued (what's driving the model-vs-market gap), shown under each comparison row. Stored as a `thesis` field per company; meant to be refreshed regularly as prices and contracted MW move (a recurring review step).
- FRMI (Fermi / Project Matador): regraded to credit the SITE SIZE — ~2 GW firm secured power (200MW grid + 1GW gas Ph1 estimated + 800MW gas Ph2 rumored), backed by the approved ~6GW TCEQ air permit + Energy Transfer firm gas + 1.5-2.2GW turbines ordered. Credited as raw-power optionality (pre-tenant, contracted 0%), NOT built/contracted; 11-17GW nuclear/SMR ambition credited at ~zero. Target −60% → −16%: the firm 2GW roughly supports the price, the premium is the ambition + OpenAI-anchor optionality (the re-rating catalyst).
- Engine: SOTP / holdco support — a company can hold a live look-through `stake` ({tk,pct}) in another tracked name (pct × its modeled equity, auto-updating) and an `eth` treasury marked to a live ETH price (Coinbase). New `holdco` model (no sites; value = stakes + treasuries + legacy − net debt). Facts panel shows the SOTP breakdown and flags when a stake alone exceeds the whole company's market cap.
- Universe → 19: added ABTC (American Bitcoin — BTC treasury/miner, tracked so HUT marks its stake live), CLSK (CleanSpark — real but 100% pre-contract AI/HPC landlord pivot; Meta 'frontrunner', ~1.1GW TX/GA, 13,470 BTC), BTBT (Bit Digital — modeled as a holdco: 70% WhiteFiber/WYFI look-through + 158k ETH; trades at a deep SOTP discount, stake alone ~1.5x its market cap → +219%).
- HUT: now tracks American Bitcoin (ABTC) as a live ~55.3% look-through stake, REPLACING the old generic $600M legacy (which the audit showed was already standing in for ABTC/mining — avoids double-count). Net slightly lower (−67%); the $7.5B nonrecourse campus debt still dominates.
- Universe expanded 5 → 16 names (multi-agent first-principles research + adversarial verify). Added GLXY, HUT, CIFR, MARA, CORZ, WULF, APLD, SLNH, FRMI, WYFI, HIVE. DROPPED FUFU (pure BTC miner, zero AI — confirmed), CLSK (near-pure miner), KEEL (not a valid AI ticker); HELD BTBT (its AI lives in the separately-listed WhiteFiber/WYFI stake — adding both double-counts; BTBT itself is mostly an ETH treasury). BTC counts reconciled to dedicated research (WULF→0 post-liquidation, MARA 36.3k, CIFR 1.5k, CORZ 547). SLNH cut to realistic ~280MW AI + $750M dilution (was an +861% artifact). HUT $7.5B confirmed nonrecourse SPV project debt (parent recourse ~$0) — correctly netted vs the gross-credited campuses → thin levered equity (−66%). MARA (AI-MW overstated, ~200MW real) and FRMI (pre-revenue power-site optionality, richly priced) kept as flagged signals.
- Cleanup / consolidation pass (cold-read traceability audit): the one binding formula documented in spec §5/§5a; removed dead/inert fields (`contractRate` constant, `renewalProb`, per-name `costOfDebt` — discount is now the single `disc` dial); collapsed to ONE legacy convention (`legacyEV` = non-BTC residual, `btc` marked live; dropped `legacyExBtc`); panel shows `mtm` for landlords / term for owners only, relabels `leaseQ` as ranking-only, and shows a sourced `basis` note on every judgement input (plannedRaise, equityDiscount, non-Proven tier). Fixed the value-bridge caption to divide by FUNDED shares (was raw shares). No target moved — pure decluttering.
- Engine: funding-dilution mechanic — per-company `plannedRaise` (realistic ATM/equity issuance, research-grounded) × a global `dilutionStress` dial; target = built-out equity ÷ funded shares (shares + raise ÷ live price). Replaces the earlier full-capex approach (which double-charged capital the multiple already embeds). Raises: CRWV $15B, NBIS $10B, IREN $8B, BTDR $0.575B, RIOT $0.25B. Board: CRWV +56%, NBIS +16%, RIOT +13%, BTDR +75%, IREN +217%.
- Engine: geography rate factor — regions carry a `rateMul` (US 1.0; EU 0.70, AU 0.70) so non-US AI capacity isn't valued at US GPU rates (Spain/Australia).
- IREN: rebuilt to its real ~5.8 GW global pipeline (was 2.2 GW) — Childress (→750MW incl. mining→AI conversion), Sweetwater 1+2 (2 GW), Kiowa OK (1.6 GW), Nostrum Spain (490MW, EU, rumored), Bundey S. Australia (800MW, AU); BC corrected (Prince George AI, Mackenzie mining→AI, Canal Flats → mining/legacy). Shares 357→373M for the all-stock Mirantis + part-stock Nostrum acquisitions; Mirantis software credited ~$500M in legacy. Near=firm/far=rumored grading. Target ~$146 (+217%, post-dilution).
- Live BTC price: legacy marks each holder's BTC treasury to a live BTC spot (Coinbase, hourly + manual ↻): legacyOf = btc × live price + `legacyEV` residual. RIOT 15,680 BTC; BTDR liquidated (no btc); IREN 0. BTC spot in header note + per-name treasury line.
- RIOT: re-rated to IG (cap 9.0% → 7.75%) — credits the scarcity / irreplaceability of Corsicana's ~1 GW powered, Tier-3-built campus (crown-jewel; fast time-to-online once leased). NOI kept as-is (AMD is a retrofit, not forward economics); Corsicana keeps full bull credit. Target ~$32 (+16%).
- BTDR: governance / control discount (15% off equity) via new per-company `equityDiscount` field + a "Gov. disc" step in the value-bridge waterfall. Reflects Jihan Wu ~75% voting control, foreign-private-issuer, controlled-company status, index exclusion. Target $37.53 → $31.90 (+101%).
- Engine: landlords now ride the GPU rate trend too (symmetric with owners) + base trend tuned +6% → +3%. Landlord NOI/rent escalates by vintage — contracted rent locked at today's level, uncontracted/future capacity earns the prevailing rent at its energization year (rising GPU $/MW → rising rents). Fixes the asymmetry where only owners re-rated on the trend, which had left RIOT frozen as the lone negative. At +3%: RIOT $26.37 → $28.42 (−5% → +2%; Corsicana's 2029/2031 buildings now earn rising rent), BTDR $35.89 → $37.53. Per-site NOI math shows today → vintage rent.
- Engine: vintage-rate model + "GPU rate trend / yr" dial (base +6%/yr, bull). $/MW·yr is now priced at the rate prevailing when capacity is contracted/energized, and that rate trends over time. The contracted share is locked at today's rate; the uncontracted share floats up to the future prevailing rate at its energization vintage — so unsold owner capacity is the upside in a rising-rate world, not a spot-haircut markdown. Replaces the owner spot "realize" (renewal × mtm) haircut; execution/lease-up risk now lives in provenance. Net relationship: the real discount on future compute = discount − trend. At +6%: IREN $85→$125, NBIS $307→$361, CRWV $182→$198. Landlords (RIOT/BTDR) are GPU-rate-insensitive (cap-rate driven) so they don't ride the trend — a known asymmetry to revisit. Per-site math and commercial facts now show the vintage rate + trend.
- Dashboard: hide the tier pill when a name is at the base "Proven" tier (only show it once re-rated to IG/IG-REIT) — removes redundant "PROVEN" labels; full page still shows the tier.
- IREN (IREN Ltd / Iris Energy): added as 5th name (multi-agent workflow + adversarial verification). Owner / GPU cloud (owns the GPUs; Microsoft ~$9.7B + NVIDIA ~$3.4B are cloud contracts, not leases). Contracted 20% (Microsoft a non-representative anchor per mgmt → spot-tilted); net debt ~$3.85B current (incl. Jun-1 $3.65B IG GPU financing); legacyEV $500M (mining fleet; BTC treasury = $0, sells daily); 13 phased AI sites / 2.2 GW owned cheap power (existence-credited). Target ~$85 (+85%) — but PRE-funding-dilution: a ~$20B+ buildout funded largely by the $6B ATM + converts will dilute the per-share number heavily (the dominant caveat).
- Navigation: hash routing — each company's full valuation is its own URL (#TICKER, deep-linkable/shareable); #sites and #sites=TICKER for the sites tab. Removed the slide-out panel — clicking a dashboard row goes straight to the full page. Back button / Esc return to the dashboard.
- All-sites page: filterable by ticker (the full page's "where the value comes from" links to #sites=TICKER); each row is expandable to its per-site math (moved off the full page). Removed dead panel/per-site-bar code.
- Build-out chart: hover any column for a tooltip listing the sites online by that year, grouped under disclosed / estimated / rumored with MW each.
- Full-page valuation redesign: a build-out-over-time chart (cumulative capacity/value by energization year, stacked by provenance, MW ↔ $ toggle) as the headline, plus a value-bridge waterfall (Sites → Legacy → Net debt → Equity → target); per-site expandable bars kept as the drill-down. Hand-rolled SVG, dependency-free.
- Live prices: Price column added left of Target on the comparison dashboard, fed from Finnhub (`config.js` holds the token), refreshed hourly plus a manual "↻ refresh" button. Live price drives the upside calc (target ÷ live price − 1) and the panel/full-page price; falls back to the manual `data.json` price if no token or the feed is unavailable. A status note shows the last refresh time.
- Header notice updated from "Illustrative placeholder data" to a present-tense, first-principles disclaimer (all four names now researched).
- Provenance discipline fix (landlords): provenance = existence (land/power/permitting/construction), NOT leasing. Owned/approved/scheduled capacity is `disclosed` even when unleased; leasing risk lives in `contracted %`. Re-tagged RIOT (all owned/approved → disclosed) and BTDR (owned → disclosed; Clarington stays rumored, litigation-stalled). RIOT $10.95 → $26.37 (−7%); BTDR $19.76 → $35.89 (+101%, cheapest per owned-MW of the landlord pair).
- RIOT: real researched data replaces placeholders (multi-agent workflow + adversarial verification). Landlord (colocation) — sole signed AI lease is AMD 50 MW at Rockdale (10-yr); Corsicana ~1 GW built on spec, zero tenants. Net debt +$636M (0.75% converts + BTC-backed loan; BTC excluded); legacyEV $1.36B (15.7k BTC treasury + FCF-negative 42.5 EH/s mining fleet + ESS Metron, BTC-spot-sensitive); contracted 5%; 7 phased AI sites / 1.04 GW, ~all estimated/rumored. Target $10.95 (−62% at $28.50) — first-principles read flags the +90%-YTD AI-pivot run as pricing far more optionality than is contracted.
- Engine: investability-tier / re-rating axis — base case for every name is Proven (the cap/multiple dials); each company can be taken UP to IG or IG/REIT grade (landlord cap-rate compression, owner multiple expansion), baked into the target. Never marked below Proven — provenance + contracted % already carry execution/lease-up risk (no double-count). Tier shown as a pill (board + one-pager), in commercial facts, and in the per-site math. All four names default Proven. Sell-side price targets are not a benchmark — valuation is first-principles from assets/income/re-rating.
- BTDR (Bitdeer): added as 4th name (multi-agent workflow + adversarial verification). Landlord model (AI colocation pivot, RIOT archetype) — flagship Tydal colo lease signed 2026-06-29 but not yet effective. Net debt +$1.66B (cheap converts); legacyEV $1.34B (self-mining + SEALMINER ASIC + BTC, low-confidence/BTC-cycle-fragile); contracted 5% (AI pivot near-zero firm); 11 phased AI sites / 1.4 GW, ~all estimated/rumored. Target $19.76 (+11% at $17.88). legacyEV built bottom-up from assets (mining fleet + ASIC business + BTC). Note: landlord path credits unleased capacity at market NOI — a known limitation to refine (lease-up haircut).
- NBIS: real researched data replaces placeholders (multi-agent workflow + adversarial verification). Net cash −$848M (prepayment-funded, convertibles); legacyEV $5.8B from non-core stakes (ClickHouse ~$4.2B, Avride, Toloka, TripleTen); 80% contracted / 4yr (hyperscaler backlog ~92% Microsoft+Meta, with high-rev-per-MW enterprise tail); 19 phased sites / 6.5 GW — scale sized on growth trajectory (Goldman cross-check $286) not headline ambition, capped 2031. Target ~$307 (+28%).
- Spec: synced §3/§4/§6a to the built model — ramp dial, site-aware contracted/spot + floor/upside split, first-power dates + rollout decomposition, expandable per-site valuation.
- CRWV: full rollout-schedule decomposition (14 → 20 site rows) — campuses phase in real announced blocks (Helios Ph1-3, Ellendale 1-2, Core Scientific Denton/Muskogee/Dalton, Lancaster A-B, Other-contracted by year), speculative pipeline spread to 2031 at ~fleet build rate. Build rates evidence-checked (Ellendale ~150 MW/yr, Helios ~300 MW/yr, fleet ~1-2 GW/yr; transformer/interconnect lead-times are the ceiling); ~75% of contracted book online by end-2027 (matches mgmt guidance). Ramp dial base 9 → 12 mo (per operator: ~9-12mo for a 50 MW hall to revenue). Target $182.
- Global dial: "Revenue ramp to steady-state (months)" — delays each site's value by N months in the time-discount, modeling commissioning / GPU fill / contract ramp after first power, without per-site phasing. Base case 9mo (CRWV target $216 → $198). Shown in each site's time-discount line.
- Full-page valuation: each per-site bar in "where the value comes from" expands to a live step-by-step breakdown (rate → margin → multiple → value/MW → gross → haircut → time-discount → site value, with contracted/expected split). Model-aware (owner vs landlord); recomputes with the dials. Panel sites stay a concise summary.
- Engine: site-aware contracted/spot split — uncontracted (rumored) pipeline earns the spot rate and moves with the GPU-rate dial; disclosed/estimated capacity carries the contracted book. New "contracted floor vs expected upside" split shown on the comparison bar and in each company's valuation build. Implements spec §4 (contracted book insulated, spot book moves).
- CRWV: real researched data replaces placeholders — 14 sites (full contracted book + ramped path to 8 GW by 2030), capital stack (~$22.9B net debt, 545M shares, $102), and sourced narrative. Target ~$218 at base case; ~21% of value rests on rumored capacity.
- Phase 1 app: four screens (comparison, sites, company panel + full page, assumptions dials), valuation engine ported from the prototype, all data loaded from `data.json` at runtime. Three names: CRWV, NBIS, RIOT.
- Scaffold Phase 1: repo, `CLAUDE.md`, spec and prototype copied in.
