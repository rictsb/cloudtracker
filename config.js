// Finnhub API token for live share-price quotes (https://finnhub.io).
// Free, read-only quote token. NOTE: this is a static site, so on the public
// URL this value is visible client-side — use a free, revocable quote token.
// Leave empty to fall back to the manual prices in data.json.
window.FINNHUB_TOKEN = "";
