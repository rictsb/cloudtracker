# Changelog

The single record of what changed. One line per change, newest first.

- 2026-08-24 — PRICE REFRESH (all 22 names, live market). The weekly sweep found every price fallback stale: the local checkout was four commits behind origin, so the portfolio bot's 8/18-8/21 marks were never pulled even though the Action ran green every weekday. Meanwhile the complex sold off hard — WYFI -42%, CIFR -39%, BTDR -36%, WULF -36%, CORZ -34%, HUT -32%, APLD -28%, KEEL -28% against our marks — while SHAZ (+17%) and ABTC (+8%) rallied. Repriced to live market rather than to the 8/21 ledger close or the price printed in a sell-side note (owner ruling: always use market price); c.price is only the fallback behind the live Finnhub quote, so it must carry a current mark. Diagnostic corroboration was three-way: the ledger NBIS mark $219.13 tied exactly to an independent 21-Aug close, and the verification agents matched our own 8/17 marks for HUT/WULF/GLXY to the cent. Upside re-expands across the book — CIFR 71->176%, CORZ 81->173%, BTDR 87->178%, HUT 41->108%, WYFI -1->+65%, CLSK 154->216%; SHAZ 103->84% and ABTC -20->-26% are the only two that fall. No model inputs touched. checks 5,262, 0 FAIL, warns 35->26 (all ten ledger price-gap warns and the ledger-staleness warn cleared). verifiedPricing 2026-08-18->2026-08-24.
- IREN caught up to EDGAR through 18-Aug-2026 — the record had stopped at the 20-Jul 8-K and missed four weeks. Every item verified against the filing itself, not a summary. (1) 13-Aug 8-K Ex-99.1: HORIZON 1 DELIVERED TO AND ACCEPTED BY MICROSOFT, the first of four 50MW-IT liquid-cooled deployments under the five-year $9.7bn contract, with Horizons 2-4 targeted later this year. Acceptance is the revenue trigger and it lands in 2026Q3 — confirming the modelled tranche schedule and making Horizon 1 the only tranche whose acceptance is a fact rather than a plan. NVIDIA Exemplar Cloud status achieved on GB300 NVL72. 480MW (gross) 2026 and 1.2GW (gross) 2027 both reaffirmed verbatim. (2) Mirantis, Inc. merger CLOSED 3-Aug-2026 (agreement 4-May-2026): ~12.6m ordinary shares fixed at signing plus ~$40m of cash, RSUs and other consideration; 11,981,668 shares registered for resale on the 4-Aug 424B7 (Reg. No. 333-284369), and the 18-Aug Form D is the Rule 506(b) notice for the same equity leg. shares 427->439.6M — the prior 427 build reconstructs as 409 + the 18.2m Co-CEO RSU grant with no Mirantis leg, and verified.capital stood at 3-Aug, the closing date itself. IREN target $289.28->$283.35 (-2.05%) purely on that dilution. Mirantis also underwrites the managed-services leg of the ramp's new attach line: IREN now OWNS the orchestration layer (k0rdent AI, inaugural NVIDIA AI Cloud Ready partner, 1,500+ enterprise customers) rather than licensing it, and states it has already facilitated several announced and prospective AI Cloud contracts. (3) FY26 results confirmed for Thursday 27-Aug-2026, 5:00pm ET — the outlook diary carried an unconfirmed 28-Aug. Two passive 13Gs (BofA 3-Aug, Goldman 12-Aug, both event-dated 30-Jun) are not recorded; the ledger does not track passive stakes. checks 5,262, 0 FAIL; page renders clean, 0 console errors. ?v=53->54.
- IREN unsigned book MARKED TO MARKET (owner-approved 8/18/26; SemiAnalysis CoreWeave/Nebius 2Q F12/26 review). The deck priced unsigned capacity below the oldest and most heavily financed print in its own book, and let $/kW-hr slide across generations — an unstated decay assumption. Against July-2026 five-year contract prints for the matching SKU, every unsigned vintage was 17-42% light: Rubin-class at $4.50-4.60 against a $7.50-8.00 VR NVL72 print, and next-gen 2030 priced BELOW today's Rubin. Re-marked at the LOW end of each range: liquid Blackwell $3.70->$4.40, air-cooled Childress retrofits $3.20->$3.81 (the note prints liquid NVL72 SKUs only, so the existing air/liquid spread is held rather than paying liquid money for air-cooled kit), Rubin-class $4.50-4.60->$7.50, next-gen $6.40->$7.50. SIGNED RATES DO NOT MOVE — Microsoft $2.91 and NVIDIA $3.10 are printed facts, and they now sit 33-46% below market, which is what IREN traded for anchor-tenant financing and precisely why the unsigned book should not inherit that discount. Spot held FLAT from 2027 at $2.40 (was decaying $2.40->$1.80, -25%): prior-generation silicon is recontracting at or above prior rates, with CoreWeave signing 2020-era A100s out to 2029. New `attach` field at 5% for storage, CPU, networking and the managed layer, applied to UNSIGNED tranches only — signed rates were back-solved from all-in contract dollars and already carry it, so an uplift there would double-count; sized on CoreWeave's own disclosure (storage+CPU+networking >$400M ARR on a ~$10.3bn run-rate = 3.9%, plus managed inference >=$250M by exit-26 = 1.3%), NOT on storage list prices. Density chain note now states that the wide Blackwell GPUs/IT-MW spread is COOLING, not noise. Effect: CY2028 $9.63->$12.15bn, CY2029 $15.00->$20.84bn, CY2030 $21.67->$31.10bn, exit ARR 2030 $23.93->$34.13bn, 2030Q4 blend $3.77->$5.38. BACKTEST UNCHANGED at MAPE 11.0195% / bias -0.2186% — verified by execution, not assumed: every tranche in the 2025Q3-2026Q1 window carries signed>0, so nothing re-marked touches it. Firewall verified by differential: IREN target $289.2842 identical before and after; engine.js not in the diff. checks 5,262, 0 FAIL. 70.9% of CY2030 revenue still carries no IREN signature — the rates are market prints for the silicon, not contracts. ?v=52->53.
- 2026-08-18 — rate dial 14 (was 13; owner-approved per SA 2Q26 neocloud review: NBIS new deals >$20M/MW vs $12M 2026 base, CRWV +25% SKU repricing applying to renewals AND net-new per owner ruling; short-tenor $40-50M/MW treated as scarcity spike, not dialed); verifiedPricing 2026-08-18
- 2026-08-18 — CRWV: signedRate 9.3->9.7 ($104B 2Q backlog re-inference), termYrs 6->5 (SA/DDTL 5.5: contracts avg ~3yr, anchors 4-5yr — owner ruling), Other-2026 500->900MW (YE guide >1.85GW); NBIS: contractedPct 80->85 (four >$1B TCV deals). Targets: CRWV $189->$228 (floor $90->$83 — shorter lock shrinks the contracted slice), NBIS $406->$426 (floor $170). Watch items: CRWV DDTL 5.5/HY-notes reconcile at 10-Q; NBIS RPO print. plannedRaise trims NOT applied (not approved)
- 2026-08-11 — verification pass (EDGAR): RIOT 8-K/10-Q confirm every frontier-lease term; converts moved to if-converted (deep-ITM $594M out of netDebt -> ~$290M NET CASH, was double-counted vs FD shares), sharesReported 375.3 (cover page); tenant reported as Anthropic by Bloomberg (UNCONFIRMED, so labeled); FRMI 8-K NOT yet filed (watch ~8/14) + unnamed 'global leader in AI' guaranty noted — RIOT floor \$15.53, FRMI floor ~\$2.9
- 2026-08-11 — RIOT: frontier-AI-lab lease booked (riot-frontier: 191 MW IT / $9.1B / 20yr / NOI $2.03M per MW-yr mid, Ph1 Dec-27 + Ph2 Jun-28 site rows); Corsicana full-campus single-tenant LOI annotated (756 MW IT ceiling, >$1B/yr rent per call); Q2 balance sheet marked (netDebt 53->294, BTC 15,680->11,380); narrative/watch items refreshed — floor $4.31->$14.30, target $43.54->$53.52
- 2026-08-11 — FRMI: FIRST binding lease booked (frmi-tensorwave: 222 MW facility / $6.5B / 15yr, NOI est. 75% margin = $1.46M per MW-yr, conditions: guaranties+financing — watch item to revert if they fail); 222 MW carved from gas Ph1 into leased row; shares 630->640.5 (7/29 LSE admission), price fallback 8.60->6.18 — floor $0->$2.77, target $12.03->$11.70 (lease +~$2B EV, offset by raise dilution at the lower price)
- 2026-08-05 — dashboard: execution floor promoted to its own sortable Floor column (floor $ + floor-vs-price %, mirroring Target/upside); floor line under Target removed; hidden on mobile like the gauge (v52)
- EXECUTION FLOOR on the dashboard (owner thought-exercise → Option B, approved 8/6/26): new `floorTarget` in the shared engine — the target with the unsigned/expected slice ZEROED (signed contracts + legacy, net of the same claims/equityDiscount/dilution) — rendered as a dashed pine tick on every gauge bar, a floor-$ + floor-vs-price line under each target, a universe floor stat in the header, and a legend entry. Reading: CORZ floor +56% and APLD +60% ABOVE price (cheap on signed paper alone); IREN floor −41% (91% of its target is the execution bet leaseUp=1.0 awards); FRMI/KEEL/NUAI/SLNH floors ≈ $0. Visibility only — no valuation change; thesis (leaseUp 1.0) untouched. Options A (per-name execution factor) and E (Outlook-window decay) remain open for a pilot on IREN/MARA.

- PREVAILING GPU RATE DIAL 8 → 13 (owner-approved 8/5/26 — the largest dial move in the tracker's history; verifiedPricing stamped): the $8M was a Hopper-era 5yr-IaaS relic. Evidence stack, all current: SA 8/5 report — NBIS ">$20M/MW at current rates", IREN expected $15-20M ARR (Sweetwater bulk deal expected at $14M = $70B/GW/5yr), Jane Street short-tenor ">$15M", short-term 50MW clusters at ~2x year-ago revenue/MW, utilization >95%; our own 2026 registry prints $14.5-18.9M (HIVE Bell 14.7, SHAZ third-party 16.7-18.9, IREN NVDA 11.3). $13M = conservative blend of bulk ($14M) and short-tenor ($15-20M). Hits owner unsigned + re-sign slices only (signed slices stay locked at signedRate; landlords untouched). Board: IREN $177→$271 (+53%), HIVE +46%, NBIS $314→$406 (+30%), CRWV $152→$189 (+24%), SHAZ +10%. OPEN (not applied): SA's "IREN cloud perceived inferior" line argues a genAccess trim there — owner to rule separately.

- BTDR TYDAL EFFECTIVE (PR 8/4/26; replaces the June signed-not-effective placeholder): $4.7B/16yr modified gross with Volta (NVIDIA Cloud Partner; end customer = a leading AI lab; Dell tech provider) — 121 IT/133 gross MW, $2.4M/MW·yr avg revenue × ~90% NOI margin = $2.16M NOI/MW·yr, THE NEW TOP PRINT on the registry tape (prior: GLXY 2.06 / WULF-Anthropic 2.01), on ~$4.0M/MW remaining conversion capex (~54% NOI-on-cost); 3% escalators; 8-yr option → $8.0B/24yr (excluded); TENANT NO-FEE TERMINATION AT YEAR 10 loudly flagged (guaranteed-ish ≈ $2.7B/10yr) vs ~$1.3B JPM-arranged LC backstop (~28% of TCV) + BTDR milestone-termination right; Ph1 12/31/26 / Ph2 3/31/27; EXPANSION per owner instruction: 2 add'l halls 47MW gross (43 IT) as a disclosed 2H27 row (campus 180 gross fully mapped); contractedPct 5→9; zero equity issued; financing (MS/Barclays/Northland) → committedDebt watch. Target $31.97-era → $33.40 vs the stale $17.88 mark (+87%); Outlook vindicated again (BTDR 80%, Jul-Sep window, effectiveness guided within-a-month — landed 8/4).

- SHAZ NVIDIA block re-marked FLOOR → EXPECTED (owner ruling 8/3/26 — should not be on the bare minimum): totalRevM 4,880 → 6,900 (SA mid scenario: block placed at market, ~$3.29/GPU-hr 6-yr avg after NVIDIA ~50% share of above-floor spread) — the FIRST expected-value override in the registry, loudly labeled on the record with a re-mark rule (actual placements replace the forecast slice with fact; REVERT to the $4.88B floor if prints land at/below it). signedRate 13.5 → 16.8, termYrs 5.6, book $8.63B → $10.65B. Target $77.98 → $91.81 (+81% at $50.70).

- SHAZ same-day filings pass + Outlook integration (owner: do it now, instead of waiting for the sweep): capital stack FILINGS-CORRECTED — Class A 35.67M filed (SA model was stale: the Dec-25 notes were $103.4M principal at $199M FAIR VALUE and 94% CONVERTED 6/11/26 at $12.53), 2032s $691.7M filed (not $700M), 2031s $48.24 confirmed and ITM at $50.70 (KNIFE-EDGE — FD 50.6M above/43.3M below the strike, watchItem), NO post-raise cash filed anywhere (Q2 10-Q ~Aug 14 replaces the last estimate; netDebt −600 medium-confidence), verified stamps set; deSPAC date corrected (closed 12/17/25, Feb-26 = listing); Bleecker short (4/30) logged on the ESDS record ($250M/yr vs $69.5M assets, unrebutted), leaseQ 3→2.5; S-1 7/31 = 26M resale shares (the −5% Friday; Aschenbrenner/Situational Awareness 19.9% underwater at $68.75 basis). EARNINGS AUG 6 AMC CONFIRMED (Delaware domestic quarterly filer — not an FPI). Outlook rows added: P(lease) 80% Sep-Dec 10-20MW exec 3.5; earnings +1.5 upside (keyNumber = the 6/30 balance sheet + active-MW, NOT revenue). Target $90.70→$77.98 (+54% at $50.70) — the share-count truth cost $12.72. NUAI/TCDC watchItem RESOLVED: stale news, 100% NUAI-owned since 1/16/26, model already correct; Oct-8 reframed (derived date, lender election, <$50M second trigger; ATM extended 7/17); ~20% share relabeled unverified inference.

- WEEKLY SWEEP 3-Aug-2026 (13 research desks, 21 names + 6 commentary feeds + pricing dials; owner-approved same day). **Two capital-structure events the last sweep missed, both filing-grade.** GLXY: Galaxy Helios Data Centers II closed $3,507M of 9.875% senior secured notes due 2031 on 7/28 — committedDebt 0→3507; the same 7/22 bond deck gave the FIRST hard print of Helios economics, term-average NOI $2.06→2.42/MW·yr (Ph II schedule sums to ~$9,419M over 260MW×15yr) with ~90%+ margin, 3-5% escalators and a 13.7% starting yield on cost, independently corroborating the SemiAnalysis TCV tape we had been discounting — net target $45.57→$43.76. IREN: 8-K 7/1/26 granted 9,099,328 RSUs to EACH Co-CEO = 18.2M units, shares 409→427 (−2.8%). **APLD Macquarie watchItem CLOSED and it was materially larger than carried** (FY26 10-K, 7/29): MAM funded $1.825B of the $5.0B A&R UPA across four closings (not $900M), redeemable NCI $1,956M (the BOOK figure — not what the model carries, see below), MIP holds 13.5% of TopCo 2 FD common (our basis said 10%, the Jun-26 deck said 15% — both wrong); TopCo 2 perimeter is NORTH DAKOTA only, so Delta Forge 1/2 sit OUTSIDE. The 1.80x MOIC floor ($3,285M on $1.825B funded, inclusive of common) already EXCEEDS the $1,956M book and binds on any redemption from 4/6/28 — **carried at the MOIC FLOOR per owner ruling: the contractual claim, not the accounting one, is what stands ahead of common**; book recorded in basis as the superseded alternative. seniorClaims 1100→3285, target $62.95→$58.02 (−7.8%), claims 5674→7728. The floor GROWS WITH EACH DRAW — ~$3.2B of the $5.0B programme is undrawn at $2.25M/MW, and a fully-drawn programme carries a 1.80x floor of $9.0B, so this re-marks quarterly. netDebt 834→703 restated to one as-of date (5/31/26), with the ~$2,150M ComputeCo-2 escrow allocation flagged as inference. Also retired: the REIT-conversion claim in narrative/bear/catalysts — the FY26 10-K contains ZERO reference to it. **CORZ re-derived off the first FILED 6/30 balance sheet**: netDebt 3380→1495 (the JPM/MS term loan was repaid and TERMINATED), shares 455→445 at a $20.72 if-converted basis — target +10.8%, but knife-edge: at $27 the 2031 converts ($22.49) and AMD warrant ($23.47) both flip and the build gives ~481M shares / netDebt ~$870M, so an as-of-price convention is still owed. **ABTC marked to UNENCUMBERED BTC per owner ruling**: 8,002 at 6/30 less 3,090 pledged to Bitmain at a ~$120,300 implied strike vs ~$62,837 spot — ABTC will rationally forfeit, so those coins are already spent on the miner fleet and marking them free double-counts against PP&E; btc 8000→4912, netDebt −50→−18, shares 70.9→72.82 (post 1-for-15 reverse split 7/2/26) — target −34.0%, and HUT −0.5% through the 55.3% look-through. **WULF Century put-floor tripwire FIRED**: Hawesville marks at ~$10.21B site EV, Raylan equity ~$6.4-6.7B net of KY project debt/capex against the ~$4.41B threshold, so 6.8% ($435-456M) overtakes the $300M floor — seniorClaims 300→450, flagged MODEL-DERIVED not disclosed. NBIS committedDebt 0→775 (first senior secured facility, $775M SOFR+2.50% MUFG-led, 6-K 7/17). BTBT stake.pct .577→.535 (27.04M WYFI shares held per its own 10-Q, not the ~29.1M the basis assumed) and eth 158461→162081 (the company’s own last primary number, 5/31/26 — the monthly PR cadence has since LAPSED). **Thesis rewrites, 6 names**: CRWV’s bull[2] inverted — the GPU-backed debt curve turned in July, newest DDTL cleared 10.44% (S+550/OID 97, 1.35x DSCR maintenance covenant) vs sub-6% in March; GLXY’s “1.6GW unleased pipeline” is really ~0.83GW gross / ~0.54GW IT; CLSK’s “8-K terms pending” was a §10 present-tense breach (filed 7/14, all terms verified); FRMI’s mid-August deadline is LIVE AND UNMET with the guide cut to ~1.5GW and capex gated on commercial progress; NUAI’s governance discount re-anchored from the departed CEO to the 7/30 Item 4.02 non-reliance restatement; APLD’s counterparty clause re-cut (810MW/~$20B now sits with a single high-IG hyperscaler). **Pricing dials RE-VERIFIED, no moves** — both 7/2 tripwires resolved negative: H100 −1.1% in July then +1.8%/7d (~−3% off the June peak, not −7%), and the new AIaaS entrants repriced the EQUITY MULTIPLE not the rent (H200 +2.3%/7d, B200 exactly 0.0%, MI300X +3.1%). rate 8 independently re-derived from IREN’s 7/20 8-K to within 1% ($4.0bn ÷ 480MW gross at PUE 1.20 = $10.00M/MW·yr vs stored signedRate 10.1); gpuTrend 8 sits BELOW the observed ladder (H100→H200 +12.9%, H200→B200 +34.3%) and stays until a Vera Rubin print exists — none does anywhere. OPEN FOR OWNER: rateTrend 3 is intact as the ESCALATOR but the engine applies it to forward/unsigned landlord space, which is the new-signing market and is compounding +7 to +14.5%/yr (CORZ vintage-on-vintage $1.441→$1.764M/MW·yr) — a spec question, not a re-verification, so unmoved and conservative. **Outlook regenerated** (STEP 2b), asOf 8/3, 22+22 rows: FIVE stored earnings dates were WRONG — CRWV 8/18→8/11 (IR release binding), NBIS 7/28→8/12, RIOT 7/29→8/05, CLSK 8/07→8/06, BTDR 8/12→8/10, and CORZ already reported 7/28. Biggest prob moves: BTBT 82→5 (SPECIFICATION error — BTBT owns no MW; every lease sits at WYFI, which the stake already marks, so 82 double-counted), HUT 70→45 (Beacon Point fully commercialised 7/20, the obvious next print exhausted), FRMI 55→30, NUAI 55→30, WYFI 80→55, MARA 40→18. **Commentary sweep**: only mcnallie-money earned its place (27 concrete claims, mostly confirmed at primary) — and it MISSED both large capital events above, overstated BTDR Tydal (225MW/“imminent” vs 180MW/not-effective) and attributed a delivery split to CORZ’s CEO the filing refutes. zeiner 100% KEEL, nothing enterable (Moses Lake already sits above its rumored ceiling). agrippa/springbok restate filings already in the file. fransbakker and hypertech COULD NOT BE ACCESSED (x.com 402; all hypertech posts paywalled) — recorded as retrieval failures, not as silence. Three attributed quotes caught as paraphrase-grade/unsupported and blocked from entering drivers. Verification stamps: capital 19 names, contracts 20 (MARA and CLSK HELD — no filing supports their stored 6/30 as-of, both re-derive at this week’s 10-Qs; HIVE contracts held to 8/18), config.verifiedPricing → 8/3. checks 5,175 assertions, 0 FAIL, 36 warn (down from 37; dial-staleness and outlook-staleness warns cleared). NOT APPLIED, still open: the stored price block (all 21 names 9-37% stale vs the 7/31 close — the app is unaffected, it pulls Finnhub client-side, but checks.js and node-side runs use the fallback), config.ethFallback 3000 vs $1,848 live (would over-mark BTBT by ~$183M on a Coinbase outage), all SHAZ input corrections, CLSK btc 13470→13924, and 4 proposed registry additions (silicondata, pitchbook-lcd, bloomberg-news, consensus-aggregators). Cosmetic: a full-file re-serialise normalised one legacy float (sharesReported 256.0 → 256) — semantically identical.

- SHAZ (SHARON AI) ADDED — 22nd name (Richard 8/3/26, built from the SemiAnalysis full model he forwarded; SOLE-SOURCE, filings verification queued as watchItem + unset verified stamps): Australia's listed GPU cloud, the only public Neocloud that is an NVIDIA Cloud Partner WITH an NVIDIA revenue backstop. Book $8.63B TCV/5 records: NVIDIA $4.88B/6yr FLOOR on 40k GB300s/72MW ($2.32/hr avg, share-above-floor undisclosed, flagged floor-not-demand) + ESDS $1.3B + NZ AI Lab $1.3B + global tech $1.0B (prints $16.7-18.9M/MW·yr — top of the gen ladder) + $150M short book. signedRate 13.5 $-weighted, termYrs 5.5, contractedPct 85; 132MW leased AU/NZ colo sites (region au — signed slice unaffected, unsigned takes the 0.70); FD 38.1M at $45.85 (2031 converts $48.24 OTM BY A HAIR — crossing flips FD +7.3M, watchItem), netDebt −300 LOW CONFIDENCE, plannedRaise 500 vs the $2.8B gap, equityDiscount 0.10 (deSPAC/overhang/USD.AI). Target $90.70 (+98%); Coverage: 637% backlog/cap and 119% ann/cap — new #1 on BOTH columns. Cross-name: Sharon SOLD its 50% TCDC to NUAI for $74M → NUAI economics re-verification watchItem. Registry annualized $1.57B/yr independently matches SA's $1.59B fleet model.

- CORZ: THE AMD PARTNERSHIP (8-K + Q2 call 7/28/26, owner-approved same day): $14B+/530MW/15yr/2.5%-esc across five campuses — corz-amd-direct 380MW NNN ($10.0B pro-rata; Pecos lead 1H27, half-'27/half-'28 delivery) + corz-neocloud-amd 150MW modified gross with FULL AMD 15-yr credit support and no equity step-in ($3.96B); implied $1.76M/MW·yr avg revenue → 1.44 NOI (NO rent credits — self-funded ~$6B build via project bonds, ~$1B pre-committed); AMD warrant (market strike) + EXCLUSIVE RESERVATION on up to 2 additional GW at Pecos/Hunt/Muskogee (expansion direct-only, capacity unmarketable while reserved — rows annotated). Sites restructured (Pecos/Hunt split to leased phases, MW allocation flagged inference; Auburn+Dalton3 row created); contractedPct 26→50 (1.12GW ≈ company's 1.1GW/$24B book); btc 547→848 (BOUGHT 301 — reversal); tier proven→IG per owner ruling (NAMED A-rated counterparty, strongest case by the CLSK-precedent standard). Q2: colo rev $137M, 437MW billing ahead of schedule, liquidity $1.8B. Target $32.22→$44.02. Outlook vindicated same-day (had +2 surprise / 60% lease prob — landed early and 2x expected size). Watch: project bonds → committedDebt, AMD warrant → FD at 10-Q, reservation windows. RIOT cross-note logged (AMD anchor went to CORZ; RIOT prints 7/29).

- IREN reconciled to the 20-Jul-2026 ARR release (8-K Ex-99.1), audited against the page rather than assumed. **The model is consistent; four prose strings were not.** On the release's own footnote-1 arithmetic (GPU/hr x 8,760 on GPUs commissioned at 31-Dec-26) the ramp ladder implies **$3.675bn** of YE-26 ARR on **exactly 480 gross MW / 147,800 GPUs** — the release's own stated basis — i.e. 8.8% under the raised >$4bn. That residual is a RATE-DECK gap, not a capacity or contracting gap: three vintage rates come off prints dated 3-Nov-25 and 7-May-26 and so predate 'contracted pricing continues to strengthen', and the model carries no storage/ancillary line, which footnote 1 includes. The tranche rates are NOT re-cut to close it — manufacturing $3.0895/GPU-hr would fit the ladder to a management target instead of to filings and would corrupt the 2030 exit ARR the page publishes. Every hard quantity already reconciled: 480MW and 1,210MW exact, ~85% contracted (model 86.0% by GPU / 87.9% by revenue at 2026Q4), 3.998yr ARR-weighted term, $2.8bn TCV, Microsoft Horizon 1-4 to 0.13% of the $9.7bn print. FIXED — four strings the 20-July ingest never re-cut (git -S puts all of them in the original 29-Jun record): catalysts[3] still posted the SUPERSEDED $3.7B YE26 ARR as a live milestone, one column away from the log entry stating the raise; narrative, bull[0] and bear[0] all carried ~$3.1B contracted ARR (= 0.85 x the old 3.7) where 0.85 x 4.0 = ~$3.4B, which the record already computes correctly elsewhere. bull[0] also attributed that ARR to Microsoft + NVIDIA when NVIDIA does not energise until 2027Q1 and contributes zero to YE-26 — re-anchored, and bear[0] strengthened with the model's own count (~60k of 148k YE-26 GPUs earning at 31-Dec). Two count errors introduced BY the 7/20 ingest: 'ten-name AI-developer cohort' (the release's ten names are the whole customer base INCLUDING Microsoft and NVIDIA, which hold their own registry rows — the cohort is eight) and Fluidstack dropped from contracts[3].counterparty though the same-dated log lists it. Outlook tenants string un-truncated. KEPT: the ~$4.4B in the narrative is a correct EARLY-2027 figure, not stale — adding the NVIDIA 60MW tranche (25,000 x $3.10 x 8,760 = $0.679bn) gives $4.354bn. contractedPct 27 does not conflict with '85% contracted' — different denominators. Unmodelled 45% prepayments and the $7.6bn cash print both cut in IREN's favour and stay as dated watch-items for the August results. Prose only: no numeric field moves, no code change, engine byte-identical (83fcb2f5), IREN target $182.20 unchanged. checks 4,981, 0 FAIL.
- Owner request: **the cumulative total is back, and the signed share is on the chart it belongs to.** The fleet panel returns to a stacked area — the top edge is the fleet total (724k GPUs at YE-30), which four unstacked lines could never show. A stack cannot draw a crossing, so the crossing is computed from the data and stated on the plot: Rubin's band passes Blackwell's in 2030Q2, marked on the band boundary the note compares. Coverage's signed share moves off its own strip and onto the dollar bars on a right-hand percentage axis — it is a share OF the quantity already plotted, so the second scale is a decomposition of the first, named on the line rather than left to the axis title (79% -> 17%). Three ordering defects found by looking at the render, not by the gates: the signed-book line drew OVER the band-label plaques (reference lines now draw first, every label last); band labels sat on the signed line and are nudged clear; and the fleet clip stayed a half-slot past the last point, so the end-of-line total label was cut by its own reveal animation — the clip opens to the full canvas at the end of the ramp. Axis title $m/qtr corrected to $/qtr (ticks are in $B). Gates 7/7, worst contrast 4.55; checks 4,981, 0 FAIL; engine byte-identical (83fcb2f5); IREN target $182.20. ?v=49->50.
- Defects #19 and #25 closed — the two structural encoding failures. **Coverage was a 100% stack**, which normalises every bar to the same height and therefore DELETES the denominator: 'signed volume rolling off' and 'the base outrunning signings' render identically, and that is the whole investment question. Rebuilt as absolute dollars on top with the ratio demoted to its own strip below. The finding it immediately surfaces: the signed band is close to FLAT in absolute terms while the total grows — signed volume is not rolling off, the base is outrunning it. The percentage view could never have shown that. **The fleet chart was a stacked area**, in which no band can cross another — foreclosing the single event the section narrates. Only the bottom band had a flat baseline; the three above sat on wiggling floors and could not be read individually. Rebuilt as four unstacked lines on a common zero baseline with direct end labels and no legend lookup. It marks the crossover explicitly: Rubin-class overtakes Blackwell in 2030Q2 — the final year, later than the section's era annotations implied. Also: generation hues are calibrated as FILLS (>=3:1) and fail as TEXT (hopper 2.79 on paper), so direct labels read in ink with the coloured dot carrying identity; labels near the right edge anchor inward on paper plaques. Harness greyscale probe repointed after the coverage aria-label changed. Gates 7/7; checks 4,981, 0 FAIL; engine byte-identical; IREN target $182.20. ?v=46->49.
- Comparison landing screen + the signed-to-street bridge. **Comparison now states its answer** (median upside, names above market, universe market cap vs our value, contracted share of that value) above the rows, and the key moved above the first mark instead of 2,859px below it. **The gauge is rebased on a comparable quantity.** It previously scaled each row to its own maximum, so with every name undervalued all 21 bars rendered at exactly 100% width and the landing chart carried no cross-row information at all. A shared per-share scale was the obvious fix and is also wrong — per-share values are an artefact of share count, and it squashed a $1.26 name to a 0.7% sliver. Bar length is now our value as a MULTIPLE of today's price: 20 distinct widths across a 26-100% range, and the market-price line sits at the same x on every row, so the gap to the bar end is the upside and is directly comparable. **New bridge panel** decomposes the street divergence at the last deep-panel quarter (2029Q2): signed contracts $986M + modelled lease-up $2,057M + uncontracted spot $364M = $3,407M model, against $3,055M street. Only the first bar is signed; a reader who accepts it and rejects the other two lands on the street number — the argument in one frame. HONEST CORRECTION: the shared-scale gauge fix was reported complete in an earlier round but had been reverted by a `git checkout app.js` during unrelated debugging. An audit of every fix reported since found this was the only casualty (RAMP_GEO, previously found, was the other). Both are back. Gates 7/7; checks 4,981, 0 FAIL; engine byte-identical; IREN target $182.20. ?v=43->46.
- Defect #22/#36 closed — the execution story is now DRAWN. The capacity panel asserted 'the gap between the bands IS the execution story' and then made the reader subtract two step curves by eye, which is near the bottom of the Cleveland-McGill ordering and worst exactly where the curves are steepest. New panel: quarters from power on to 90% of run-rate, one row per tranche, chronological, zero-anchored, with a label column — commissioning as a neutral segment, sell-down in the generation hue. IT SURFACED A FINDING THE PAGE DID NOT PREVIOUSLY MAKE: the lag gets materially WORSE over time — median 3 quarters for the first ten tranches, 8 for the last ten — and none of it is construction. Commissioning is a flat one-quarter modelling convention throughout; every quarter of the deterioration is sell-down, because later tranches carry less contracted capacity and uncontracted megawatts take 4.4x longer to reach full rate. The execution risk in this model is a CONTRACTING risk, and it compounds. Also fixed: RAMP_GEO had been lost in an earlier revert, so the 'unified chart geometry' reported in the previous round was only partly in force — the fleet and revenue panels were still on mr:20/mt:16/mb:26 while the capacity panel was on the shared box. Restored, and all three time-series panels now derive from it. The lag panel takes a stated exception (a 196px label column) because its x-domain is quarters-since-power-on, not the shared calendar. Gates 7/7; checks 4,981, 0 FAIL; engine byte-identical; IREN target $182.20. ?v=41->43.
- SEMIANALYSIS HPC TAPE INTEGRATED (7/27 file from Richard; 157 deals, 31 in-universe; all six owner-approved actions): (1) tenant IDs adopted with attribution — Beacon Point = NVIDIA, APLD PF2 = Oracle, DF1/PF3/DF2 = Meta (SA-identified, estimated ceiling, counterparty fields annotated not silently renamed); (2) CORZ RENT CREDITS CONFIRMED vs the 4/21/26 8-K EX-99.2 — ~$879M of CoreWeave credits (capex repayment $1.5M/MW at 50% of each monthly invoice ~29mo/campus + $126M end-of-term): campus-lease NOI 1.20→1.07 term-avg, near-term NOI actually HALVED 2026-27, CORZ target $34.33→$32.22; (3) WULF Century NCI verified WITH the $300M FIXED PUT SA missed — seniorClaims 101→300 (floor; floats above $4.41B Hawesville equity, watch-item), target →$36.47; (4) CRWV Helios rows corrected to 133/260/133 IT (dual-corroborated; ~274MW over-credit removed), watch resolved; (5) APLD PF2 spec-build 100MW row added (expansion trimmed to net), SLA texture recorded (APLD uncapped service credits vs CIFR no-SLAs per Fitch); (6) CORZ CoreWeave book split to per-campus records (Denton 262/Dalton 175/Muskogee 70/Marble 65). Validations: SA Y1×escalator math reconciles to our term-avg registry to the cent on every shared deal; ~3% escalators universal (rateTrend dial confirmed); Abernathy under FS+Sponsor (matches our JV treatment). Coverage gap: SA lacks all compute contracts (~$150B) + Cerebras/Tydal/Highrise.

- Defect #8 closed — one meaning per channel. Green was carrying THREE encodings across two greens 35.4 ΔE apart: signed sentiment ('beats street'), unsigned magnitude (the backtest error column rendered BOTH −16% and +16% green), and a nominal category (Blackwell silicon). A reader taught green=good in one section read a −16% miss as a positive result. Now: HUE = silicon generation and nothing else; SIGNED values diverge on a new --pos-ink/--neg-ink pair (indigo/clay), never on green; UNSIGNED magnitude is ink weight on a neutral ladder, never hue. Generation ramp re-cut in CIELAB to fix chroma and greyscale together — the old ramp ran C* 47-78 (2.4x the print inks it sat beside) and collapsed three of four generations into an 8.3 L* band, so the categorical encoding dissolved in a photocopy. New ladder #b3864a / #35895b / #00427f / #994468 sits in the ink band with relative luminance 0.271 / 0.194 / 0.054 / 0.119 — minimum adjacent gap 0.065, and every step clears 3:1 on --card (old ramp: 2.07-2.70, all four failing). Uncontracted tints re-pinned above every solid so the contract encoding still cannot invert. In-chart band labels moved onto paper plaques: on fills of varying lightness at 0.82 opacity neither ink nor paper cleared AA on all four (paper-on-Blackwell measured 2.83), so labels now read ink-on-paper at 12.7:1 regardless of what sits beneath. Harness: greyscale gate extended to assert the generation ladder is monotone with >=0.04 separation (it now measures the tokens, not just the coverage bands); external API rate-limits (Finnhub 429) no longer fail the run — environmental, not a page defect. Gates 7/7, worst contrast 4.55. checks 4,935, 0 FAIL; engine byte-identical; IREN target $182.20. ?v=39->41.
- Defect-ledger round 1 (72-item external audit, 27-Jul-2026): verified each measurable claim against the live page before acting — several were already stale. FIXED: heading outline (measured 0 h1 / 0 h2 / 0 h3 / 8 h4 — now h1 + a visually-hidden h2 per screen, zero pixels changed); the deck out of 10.5px all-caps letterspaced mono into 12.5px sentence-case sans (the .eyebrow was the last mono prose on the page and the likely source of the owner's readability complaint); capacity axis GW->MW (4 GW ticks sat beside 44 MW mentions on one screen); the 18 scrubber dots keyed in words (power on / first revenue / milestone); SVG <title>+<desc> on all three panels stating trend, endpoints and units; en-GB orthography enforced (modeled->modelled, energized->energised, utilization->utilisation). VERIFIED STALE OR WRONG: '$23.9B denominates nothing' — the card reads '$23.9B exit ARR 2030, base case'; 'SVGs have no accessible name' — all four carried aria-label. Harness gates 7/7 after excluding screen-reader-only nodes from the density probe (a 1px .vh node is not a clipping defect). checks 4,935, 0 FAIL; engine byte-identical; IREN target $182.20. ?v=38->39.
- Readability + consistency pass (owner feedback 7/26/26): **running prose moved out of IBM Plex Mono into the sans** at 12.5px/1.55 with a 96ch measure — mono is now numbers, labels and axes only. This was the readability complaint: .legend2, tooltips, backtest prose and the answer strip were all set in 11px mono. **Comparison screen bars fixed** — gaugeHTML scaled every row to its own max, so all 21 bars rendered at exactly 100% width and the landing chart carried zero cross-row information despite a legend claiming 'Bar = our value'. Now scaled against one universe maximum, so length is comparable row to row. **Three ramp charts unified on one geometry** (RAMP_GEO: same width, margins, height, plot box, axis and right-hand label treatment) — the capacity staircase, fleet area and revenue panel were previously three different designs with different margins and heights. **Three assumption callouts added**, quantified and next to the chart each drives: build cadence (12-16 months to first power at a new campus, 6-8 weeks per extra 50MW-IT hall; IREN's best four-quarter add +550MW vs the model's 275MW/qtr average and 369MW/qtr peak), density ladder (Hopper 1.60 -> Blackwell 2.48 -> Rubin 4.23 -> next-gen 6.30 kW per package; GPU = package not die, a 2.4x fork), and commissioning + rate provenance (regime-split 1-3 qtrs contracted vs 4.4x slower uncontracted; 3 of 9 vintages printed, 63% of 2030 revenue prices off unpriced silicon). Band labels clamped inside the plot (RUBIN/NEXT were clipping at the right edge). Gates 7/7, type scale held at 7 steps; checks 4,935, 0 FAIL; engine byte-identical; IREN target $182.20. ?v=36->38.
- AUDIT round 3: whole-app design audit (first ever — earlier rounds only examined screen 11) + ramp encoding rethink + the last five ramp blockers. App scored **58/100**. **The bar-thickness question was a real defect and it was the tell.** The capacity Gantt encoded gross MW as sqrt(MW)x1.05 bar thickness — a 75x quantity range drawn across 4.3x of pixels, undocumented in the legend — while bar LENGTH, a far stronger channel, carried nothing because every bar ran to the chart's right edge. Ranked by rendered ink against gross MW, 192 of 363 tranche pairs (53%) came out backwards — worse than a coin flip. REPLACED with a cumulative staircase: energised gross MW, commissioned critical IT MW and earning critical IT MW on one labelled megawatt axis, with the 480MW/1,210MW company commitments as reference lines. The gap between bands is now the execution story, and it exposes what the old chart hid — 79% of commissioned critical IT is idle at 2026Q3, still 22% by 2029. **Three colours were carrying seven meanings across six screens.** `.prov.*` (provenance) was reused for GPU generation on Leases — vera-rubin, the newest silicon, rendered in the same clay as a lease marked 'not effective' — plus instrument type on Raises ('equity' read as 'rumored'), contract kind and earnings sentiment on Outlook, 'excluded' on Portfolio. Generation now uses the existing --gen-* scale; new .inst/.kind/.lean scales for the rest; .prov means provenance again. **621 text elements were below AA app-wide.** The --pine-ink/--clay-ink tokens added in round 1 were only wired into screen 11; every other screen still painted status text with the fill tokens (--pine 4.09:1, --clay 3.64:1 on paper). Swept — zero raw fill tokens remain as text colour. Also killed provisional-by-opacity (worst 2.09:1; it destroyed contrast on every glyph in a row and vanished in greyscale) and gave .tabs flex-wrap (nine tabs ran five buttons off-screen below 782px). Ramp blockers closed: rate provenance rewritten — three of nine vintages come off a dated print covering 13.2% of CY2030 revenue, 63.2% prices off Rubin-class or later silicon for which nobody has disclosed a price as at 23-Jul-2026, now stated per vintage with dates, tranches and revenue shares; next-gen density adopted at 6.30 kW/package (the ladder stepped +55%, +71%, then +16% — a rung no roadmap supports); wedge contrast 6-of-13 failing (worst 3.37) to 13-of-13 passing (worst 4.95); Gantt greyscale inversion resolved structurally (all 10 hatched Rubin bars had read darker than all 9 solid bars). Coverage spine switched to revenue-weighted share, the conservative reading its heading promises (17% signed by revenue vs 21% by count at 2030). Harness greyscale gate repointed from the retired Gantt bars to the coverage spine and now MEASURES rendered luminance instead of asserting a pattern exists; separation 0.649. Gates 7/7; checks 4,935, 0 FAIL; engine byte-identical; IREN target $182.20. ?v=35->36.
- AUDIT r2 REMEDIATION (5 of 12 open ramp blockers): **rate provenance no longer over-claims** — the chain's rate link said "dated contract print tape" for all nine vintages; only three are printed (MSFT $2.91 from the 3-Nov-2025 8-K, NVDA $3.10 against a $3.20 implied print from 7-May-2026, $2.48 IREN ARR disclosure) covering 13.2% of CY2030 revenue, and 63.2% prices off Rubin-class or later silicon for which NO price has been disclosed by anyone as at 23-Jul-2026 (NVIDIA VR200 production shipment 27-Jun-2026, CoreWeave rack validation 1-Jun-2026 and initial Rubin bookings, both priceless). Each rate now names the tranches it drives, its share of CY2030 revenue, and its derivation — the $4.50-4.60 band is a power-parity read ($1.06-1.09/kW-hr vs the Microsoft print's $1.106), not a tape. **Next-gen density adopted at 6.30 kW/package** (159 GPU per IT-MW, was 4.911/204): the silicon ladder runs 700W -> 1,400W -> 2,300W per package (+100%, +64%), the model's IT-boundary rungs +55% and +71%, and a +16% last rung is supported by no roadmap point (Rubin Ultra is 4.17 kW/package at 600kW/144 in Kyber; Feynman ~2kW/die in ~1MW racks). Rate rebased to $6.40 to hold $/kW-hr, so YE-30 fleet 748k -> 724k GPUs and next-gen nameplate 157,000 -> 122,300 while CY2030 revenue moves 0.04% — density is a fleet-headline lever, not a money one; exit ARR $23.93B unchanged. **Wedge-mode contrast**: --pine-ink/--clay-ink are tuned to be exactly AA on paper (4.53/4.55), so any wash under them fails — the thin-panel shade was drawn TWICE (ghost pass + reveal) and the cumulative-wedge annotation sits on a trapezoid fill. Wash moved to the static layer; all seven street-mode annotations now sit on plaques. Measured worst pair 3.37 -> 4.95, six failures -> zero. **Gantt greyscale inversion fixed**: the rghx-* hatch was hue-dependent (fill @0.3 + stripe @0.85), so all ten hatched Rubin bars (L 0.27-0.29) read DARKER than every solid bar (blackwell 0.33, hopper 0.43) — the contract encoding inverted. New spec: opaque --paper ground + a 2.4/5 stripe in four new tints pinned to L=0.55 and mixed against a neutral so no new hue enters the palette, plus a 0.8px full-hue keyline; solids go to opacity 1.0. Measured solids 0.308-0.409, hatched 0.620-0.731 — zero inversions, minimum separation dL 0.211 (1.46:1). Coverage spine (revenue vs count) was closed separately at 104fd64. Engine byte-identical (sha256 83fcb2f5...), IREN target unchanged $182.2036; checks 4,935 assertions 0 FAIL; harness 7/7, 0 console errors. ?v=35->36.

- NEW OUTLOOK SCREEN (spec §6, now ten screens; Richard's ask: who signs the next lease, who surprises on earnings): two sub-tabs on a new tab, all columns sortable, rows expand to the evidence trail. NEXT LEASES — P(new binding lease, 6mo) for all 21 names with expected MW, window, candidate tenant/site, 1-5 build-speed score, big-leases-only filter (≥100MW); top board: CRWV 95 / IREN 85 / BTBT 82 / BTDR 80 / WYFI-HIVE-APLD 70-80. EARNINGS SETUP — per-date (owner calendar seeded, confirmed/est flagged) surprise score −5…+5 RELATIVE TO WHAT IS PRICED, key metric, consensus, sentiment. Built by a 28-agent workflow (21 desks + 7 adversarial verifiers, 866 tool calls); verification CAUGHT AND CORRECTED: 2 fabricated "verbatim" quotes (IREN, CIFR — not in filed transcripts), a nonexistent NBIS 6-K (Reflection AI — cadence driver INVERTED, prob 92→75), CIFR's phantom August rent catalyst (real: delivery 9/30, rent 10/1 per the 8-K decks → earnings score 3→1, Black Pearl site rows re-dated to the disclosed subphase schedule, 7/20 log entry corrected), APLD's refuted exclusivity argument (80→70) and consensus arithmetic, CRWV date/drawdown/insider-selling restatements, BTBT ETH-mark math (−$66M→−$90M). Data: top-level `outlook` object (present-tense, overwritten weekly by sweep STEP 2b — prompt updated with the outlook step AND a quote-discipline rule born of the fabrication catches); checks validate tickers/bounds/staleness (>8d = overdue); NEVER feeds the engine. WatchItem: CRWV Helios gross-vs-IT MW. launch.json: tracker entry restored.

- IREN ramp: Nostrum first revenue 2029Q1→2029Q3, energize 2028Q4→2029Q2 (owner-ruled 7/25/26) — Spain has no disclosed hall schedule; 2029Q1 implied a mid-2027 construction start with clean Spanish permitting on a first-EU build; skew is later, not earlier. Bundey kept at 2029Q4 (already conservative vs company "energization from 2028" with signed transmission agreement). Impact: 2029Q1 -$77M / -8.5k GPUs, 2029Q2 -$152M, converges by 2029Q4; YE-30 unchanged (799k GPUs, $25.8B exit ARR). Basis note appended in data.json. Re-date trigger: FY26 results (~late Aug) construction-start/permitting language or a first EU customer.
- AUDIT round 2: four design/measurement critics + a density desk ran against the corrected page; 26 blockers filed, 14 fixed. **Arithmetic cleared** — Critic A rebuilt the chain independently and reproduced all 18 quarterly rows, exit ARR, blend, coverage decay and backtest to the decimal; firewall verified intact. **Numbers that were wrong and are now right:** rampMult was consumed TWICE (once mutating the tranche, once in mix()), so the published joint downside overstated itself by $2.06B of double-counting; the 'uncalibrated ramp' row used rampMult 0.227, which a ceil() cannot invert, and drew an INSTANT ramp (819k = exact nameplate) rather than the pre-backtest model; the Critical IT MW column ramped on the raw linear fraction while GPUs used the calibrated regime mix, so the two published columns divided to 3.87 kW/GPU against the chain's own 3.59; both scenarios labelled downside carried spotFlat, which pins spot at the 2026 level while the base curve declines — an UPSIDE lever inside a bear case. Base exit ARR $24.0B->$23.9B, joint downside $12.3B->$12.7B (-47%), flat vintages $20.4B->$19.3B. **Round 1's central fix was being undone by the default view**: the 'by generation' chart drew the consensus AI-cloud line against a bar top that still included BTC mining — the same total-vs-AI artefact, moved from the headline into the chart. The AI subtotal is now marked on every affected bar. Also fixed: the ramp screen was the only screen in the app whose main column was not 1124px (a wide table inflated the 1fr track; `main{min-width:0}`); two history tranches energised before the Gantt's axis start and painted over the campus-label column; the vs-street KPI tile read a bare em dash on every cold load because the page lands on 2030Q4 and consensus ends 2030Q3 (now falls back with a labelled quarter); the street legend called the AI-cloud line 'model (total)'; the density chain link published an idealised ladder that does not reproduce the stored GPU counts (now publishes the actual per-tranche ranges and states that gpus is stored, not derived); the ramp chain's commissioning row published a range excluding the value the shipped base case uses. Density desk resolved the two Phase-1 desks against primary sources: desk 1 REJECTED (IREN's own >19,000 GB300/50MW-IT is jointly pinned with the $2.91 rate by the $9.7bn contract — 76,000 x 2.91 x 8760 x 5 = $9.69bn), desk 2 upheld; the 'Prince George / BC expansion' tranche implied 1.12 kW/package, below a bare H200's draw, re-cut 10,700->7,500 GPUs. checks-core ramp group now BINDS: it re-derives the chain independently of app.js (all seven scenario levers), fails on backtest MAPE >25%, on a terminal IT-MW/GPU ratio diverging >8% from nameplate, on any scenario that changes nothing, and on a joint case that is not worse than its worst single lever — it caught four under-implemented levers in its own first run. 4,915->4,935 assertions, 0 FAIL. Design harness 7/7 (grid alignment 95.6->97.0). Engine byte-identical; IREN target unchanged $182.20. ?v=33->34.
- fix: GPU-ramp coverage spine actually renders the collapse it claims (self-caught on visual inspection, round 2). Two defects: the `<defs>` block was emitted as a SIBLING of the `<svg>`, so `url(#rampCovHatch)` never resolved and the 'modelled contracted' band drew as nothing; and at 72px tall the hatch read as the same dark mass as the solid band, so the 78%->21% decay in signed-today coverage — the page's most important honesty claim, and named error #6 — was invisible. Rebuilt: taller chart with a 0-100% scale, three bands separated by LIGHTNESS (solid indigo / hatched / light) so the split survives greyscale and small size, and the decay traced as a position-encoded line with endpoint labels (position beats area comparison). ?v=31->33.
- AUDIT: GPU-ramp page made falsifiable (owner-directed audit 7/25/26). **Backtest added and the model failed it, then was fixed.** Run over IREN's three reported quarters the chain overstated every one (+41%, +90%, +64%; mean bias +65%). Root cause: it billed uncontracted capacity as if it commissioned like a take-or-pay contract. Correction is regime-aware — contracted GPUs bill from customer acceptance, uncontracted capacity must be SOLD DOWN (only 11k of IREN's 23k GPUs were contracted in 2025) — fitted at 4.4x on the uncontracted leg only; mean absolute error 64.3%->11.0%, bias +64.3%->-0.2%. Base case is now the retrodicted path: exit ARR 2030 $25.8B->$24.0B. **The +78%-above-street headline was an artefact and is gone.** It compared model TOTAL (incl. mining) against consensus TOTAL, and was drawn at 2030Q3 — the one quarter where the consensus total and AI lines are identical to the dollar, i.e. a collapsed panel. Like-for-like AI-vs-AI, the model is 8-35% BELOW the street through 2027 and at most +22% inside the reliable window (to 2029Q2, where the panel thins); beyond that the chart is shaded 'indicative only'. New on the page: coverage spine (signed-today share falls 89%->21%, now a chart not a table column), backtest table, the 7-link chain with every transfer function/assumption/range declared, and a 10-row sensitivity table incl. the joint downside ($12.3B, -49%). Data fixes: Horizon 1 energize 2025Q4->2026Q2 (it did not energise in 2025Q4 — GPU commissioning was only 'underway' at the 11-May-2026 call, so the page was crediting an on-time liquid-cooled delivery IREN missed by two quarters); base fleet split into its three disclosed cohorts (totals identical) so the chain can retrodict; GPU unit declared as PACKAGE not die (a 2.4x fork if left unstated); cadence basis now states the model asks 2.0x IREN's demonstrated 138MW/qtr and 2.7x at peak; secured-vs-modelled reconciliation published per campus (Childress/Sweetwater/BC are phased above their secured figure — mostly a gross-vs-IT basis difference at Childress, ~150MW genuine at Sweetwater). checks-core.js gains a GPU-ramp group (+319 assertions, 4,596->4,915, 0 FAIL) enforcing the chain invariants, marked assumptions, and the presence of a backtest and a joint-downside scenario. Design: contrast failures 145->0 (--ink-soft darkened 2/channel; text-safe --pine-ink/--clay-ink; band labels re-inked), type scale 10->7 steps, prose raised to 11px; harness gates 7/7. Valuation engine byte-identical (sha256 83fcb2f5...); IREN target unchanged at $182.20 — the overlay remains display-only. ?v=28->31.
- fix: GPU-ramp autoplay removed (owner-agreed 7/25/26) — page lands on the full 2030 picture; the replay button remains the opt-in movie; scrub/spotlight unchanged. ?v=26→27.
- feat: global dials collapsible, collapsed by default (owner request 7/25/26; spec §6.4 amended) — sidebar hidden with main spanning full width; "☰ global dials" header toggle; preference persists per browser (localStorage cv-dials); dials fully functional when open, engine untouched. ?v=25→26.
- feat: GPU-ramp information-design pass + vs-street mode (owner-directed 7/25/26: "better information design, not more text"; spec §6 screen 11 amended) — denser signals, fewer words: every stat gained a signed q/q delta that ticks on scrub (+ a 7th vs-street stat, pine/clay coded); timeline gained era bands (PROVE 480MW → SCALE 1,210MW → COMPOUND Sweetwater/Kiowa/intl), year ticks, progress fill and a quarter chip riding the playhead; campus lane tints; fleet era markers; quarterly table gained year-boundary rules + color-coded Δ column; Money chart mode toggle "by generation / vs street" — model vs consensus lines with the wedge sign-shaded per quarter, Δ% labels, terminal labels and a cumulative wedge callout (+$14.6B through 2030Q3 at current model). Drafted hero/lede prose was cut per owner steer. Targeted 13-agent adversarial review: 4 confirmed fixed pre-push (street mode crashed for any future no-consensus ramp company; wedge annotation collided with the model line + a Δ label — reanchored to the reviewer-computed clear position, index-safe; mode toggle scroll-jumped an active spotlight; TODAY line struck era labels — gantt paint order hoisted); 6 refuted/no-action. checks 0 FAIL; ?v=24→25.
- feat: GPU-ramp rollout explorer (owner-directed "go wild" 7/25/26; spec §6 screen 11 amended) — the screen is now a time machine: timeline scrubber + auto-replay rebuilds IREN quarter by quarter across all three charts (ghost layers + clipPath reveals, rAF at 1.6 qtr/s, setInterval fallback + no autoplay under prefers-reduced-motion); live state panel (GPUs earning, gross/IT MW, rev/qtr, blend, signed %); per-quarter callouts computed from tranches (power-on/first-revenue pills + milestones: 480MW/1,210MW program targets, fleet round numbers, first Rubin/next-gen silicon, consensus crossover, mining-to-zero) with clickable milestone dots on the track; tranche spotlight (click a gantt bar → all else dims, its GPU/revenue path traces through the other charts, house-style detail card); synced hover highlights; click-to-seek from charts and table rows. Two review rounds pre-push: my own screenshot pass caught a rewrite-introduced YR off-by-one (spot year hit 2031→$0 in 2030Q4; shipped code was never wrong); the 22-agent adversarial workflow confirmed 7 lifecycle/perf bugs, all fixed (render() price-tick/dial rebuilds no longer kill playback/spotlight — ramp skips rebuild when DOM is live; play-button label derives from timeline state; scrubber thumb no longer freezes on focus; intro autoplay timer cancelled by any interaction; frame-delta clamped so a backgrounded tab resumes instead of teleporting; reduced-motion replay renders its first quarter; per-frame DOM writes gated to quarter changes). checks 0 FAIL; ?v=23→24.
- fix: GPU-ramp interface consistency pass (owner-requested review + audit agent, 13 items) — per-chart legends (fleet gained its missing legend; hatch chip no longer promised on the un-hatched revenue chart; consensus/signed-line chips are proper dashed swatches, hatch chip neutral); BTC-mining bars gold→--far (was near-indistinguishable from Hopper amber stacked adjacently); generation hexes lifted to CSS tokens (--gen-*); house bar-separation stroke (var(--card) 1.5) replaces the manual gap on revenue bars (which also floated 1.2px off the baseline), fleet area strokes 2→1.5; zero-baseline gridlines added to fleet/revenue; SVG axis text normalized to the 10px house convention (gantt was 9/8.5px); overlay dashed lines unified 6-4; gantt bars squared (rx removed) and campus order data-derived; quarterly-table headers no longer advertise sorting they don't have (.stab.nosort); h4.sec inline margin overrides dropped; fragile street-consensus in-chart label removed (legend carries it), fleet gen labels dodge the signed-book line; ?v=21→23.
- feat: GPU-ramp screen (spec §6 screen 11, owner-approved 7/25/26) — quarterly capacity/fleet/revenue path for GPU-cloud owners, IREN first. Three generation-colored layers on one time axis (Concrete: 27 capacity tranches energize→first-revenue with signed/uncontracted hatching; Silicon: revenue-generating GPUs stacked Hopper/Blackwell/Rubin/next with the signed-book line ~165k; Money: quarterly revenue by generation + residual mining vs Bloomberg consensus) + the quarterly table (gross MW cum, critical-IT MW active, GPUs added/cum, signed-today % vs modeled-contracted %, blend $/GPU-hr, revenue, consensus, Δ). Data as `ramp` reference object on the IREN record (display-only, engine never reads it — target unchanged $182.20); tranche schedule filings-anchored (480MW/150k YE-26 + 1,210MW YE-27 disclosed; 2028-30 modeled cadence), rates from the contract print tape (MSFT $2.91/GPU-hr, NVDA ~$3.10, Rubin $4.50-4.60), consensus from owner-provided Bloomberg pull 7/23. Adversarially reviewed (13-agent workflow): 4 confirmed latent bugs fixed pre-push (tooltip quote-escaping that &#39; can't survive HTML decoding, NaN% guards for future ramp models, signed-book headline recomputed from tranche sums not Q[3], campus order + fleet labels data-derived for CRWV/NBIS extension); 6 findings refuted. checks 0 FAIL; all tabs regression-tested; ?v=20→21.
- IREN site-schedule reconciliation (deep research 7/23/26, owner-approved): quarterly GPU-ramp study reconciled data.json against filings/8-Ks. Horizon 5-6 added (100MW disclosed, 2027-09 — in the company's 1,210MW YE-27 build, was missing); Childress conversion A 240→140MW and estimated→disclosed (air-cooled conversion is in the disclosed 2027 program; keeps campus at 750MW); Kiowa ph.1 2028-09→2029-06 (SPP queue entry only Mar-26 + transformer leads make 2028 revenue unrealistic); Nostrum rumored→estimated (acquisition COMPLETED 6/15/26, 490MW grid-connected owned); Bundey ph.1+2 rumored→estimated (campus announced 6/3/26 with signed transmission connection agreement, energization from 2028). Horizon 1-4 dates kept — spec §4 defines site yr/mo as first-power; the MSFT revenue handoff lag (H1 energized Q4-25, revenue Q3-26) is the ramp dial's job, noted for a future spec question. Target $160.02→$182.20 (+14%, mostly the Nostrum/Bundey 0.25→0.55 existence haircut per spec §4 "provenance is existence, not leasing"; Kiowa slip partial offset).
- HUT: BEACON POINT FULLY COMMERCIALIZED + IG RE-RATE (8-K 7/20/26, owner-approved): second $9.8B/15yr/352MW-IT NNN lease with the SAME undisclosed high-IG tenant (now 704MW/$19.6B at the campus; 3.0% escalators, ~$655M/yr avg NOI — identical $1.856M/MW·yr print to Ph1; three 5-yr options → $50.2B potential; first Ph2 hall Q2-28). Registry + site row (Ph2 350 estimated → 352 disclosed/lease-linked); contractedPct 46→45 (registry-derived — rumored ROFO/Vega in the denominator); tier proven→IG on owner ruling (basis: tenant COMPANY-DISCLOSED 'high-investment-grade', 100% of contracted AI capacity IG-leased/backstopped per the exhibit, Google backstop on River Bend, IG project debt; FALSIFIER: tenant identity sub-IG or exhibited terms diverging). sharesReported 112.6→123.2 (buyback math: 6,159,439 = 5.0%). Target $123.38→$162.96; book $16.9B→$26.6B. Watch: Ph2 financing → committedDebt. CIFR same day: NO deal (EDGAR-verified sympathy move) — log note only: AWS Black Pearl Ph1 delivery begins July, rent commences August.

- IREN $2.8bn AI-developer contract wave (8-K 7/20/26, owner-approved same day): new aggregate registry record — Perplexity/Figure/Together/Fireworks/Fal/Hume + one unnamed developer, $2,800M TCV, term ~2.5yr INFERRED (backed out of the filing's ~4yr ARR-weighted portfolio average: (14.1+2.8)/(3.12+2.8/T)=4.0), ~110MW inferred, ~45% customer prepayments on GPU capex. Book: $14.1B→$16.9B TCV, ~$4.2B/yr annualized at full ramp (company YE-26 contracted ARR ~$3.4B — Horizons ramp gap). termYrs 5→4.0 (COMPANY-DISCLOSED wtd-avg replaces placeholder; TCV-weighted 4.4 alt declined, ±$0.30); contractedPct 20→27 (scaled by contracted-ARR growth +35%; MW-derived 11% alternative noted). ARR target $3.7→$4bn+ (85% contracted); $7.6bn cash 6/30. Target $156→$160 (+2.5%), IREN stays #1 (+283% at live $40). Watch: unnamed developer identity; netDebt re-verify (one as-of rule); plannedRaise $8B stale-conservative vs prepay model — revisit at FY results.

- CIFR: BTC treasury 1,500 → 1,116 (Cipher Q1'26 10-Q, held as of 2026-03-31; verified via EDGAR + adversarial check). The stale 1,500 was a third-party-tracker figure frozen at Oct-2025; management is divesting toward full exit by YE2026, so the true count is likely lower still but no post-10-Q disclosure exists. Trims CIFR's marked treasury ~26%.
- MARA: reconciled BTC prose 35,303 → 36,303 to match the `btc` field and the latest disclosure (mid-month update, held as of 2026-06-16; 35,303 was the May-11 figure, now stale). Also corrected `basis.plannedRaise` ATM date Mar 2026 → Mar 2025 (the $2B ATM is the 424B5 filed 2025-03-28; no 2026 ATM exists on EDGAR). No engine change — the `btc` field was already 36,303.
- CLSK tier ruling settled (Richard 7/14/26): STAY PROVEN — IG re-rate (+$4.20/sh) declined; future trigger = 8-K-confirmed terms + a second IG lease (TX exclusivity converting).
- CLSK SIGNED ITS ANCHOR (PR 7/14/26, 8-K pending — entered same-day per owner ruling): 20yr NNN lease, 175MW IT at Sandersville GA, $6.6B initial term (~$330M/yr avg NOI at ~100% margin, annual escalators; two 5-yr extensions → $11.6B), deliveries from Q4-27, landlord cost $10-12M/MW; tenant confidential "high-investment-grade cohort" global tech, and the SAME tenant holds LOI + exclusivity over CLSK's entire 718-acre/885MW Texas portfolio. Print $1.886M/MW·yr term-avg — above the $1.76 forward anchor, second 20yr term on the tape. Owner rulings applied: residual 75MW Sandersville row KEPT (rumored); Brazoria expansion 300MW rumored→estimated ("it will definitely energize" — exclusivity mandate, annotated); plannedRaise held $300M (build expected project-financeable → committedDebt when it prints); tier held PROVEN pending ruling on the IG re-rate (quantified: +$4.20/sh, $39.10→$43.30). Board: CLSK $25.48→$39.10 target (+154% at the old $15.37 mark). contractedPct 0→15; watchItem: 8-K verification, tenant identity, financing, TX LOI conversion.

- HONEST GENESIS: the paper portfolio's backward year now runs on `data-pit.json` — 158 dated point-in-time snapshots across all 21 names, each rebuilt from disclosures published ON OR BEFORE its date (42-agent reconstruction+audit fleet; the independent audit caught and fixed ~130 look-ahead violations — IREN 15, RIOT 12, CRWV 11 — e.g. leases before signing, energization dates from later guidance, share counts from later filings; one post-audit correction: ABTC's 7/2 snapshot had leaked its 7/7 reverse split). Result: reconstructed genesis NAV 3,381 vs equal-weight 265 (base 100) — read with BOTH stated caveats: (1) universe selection is today's (survivorship: NUAI, the largest contributor at +1,825 NAV pts with a 93% peak weight, is on the list partly BECAUSE its project worked), and (2) paper fills at close prices ignore liquidity (no cap by mandate → repeated >50% positions in sub-$100M microcaps no real book could hold). The genesis is labeled 'reconstructed' everywhere; live records (Jul 6-9) were preserved and re-chained return-preservingly. Regeneration: `node portfolio-backtest.js --pit` (reattaches live records by default; --force discards). NEW UI: genesis on/off toggle [Incl. genesis | Live only] on the Portfolio tab — live-only shows the daily record from go-live (chart, absolute returns, attribution, stats all scope). Spec §6b files + provenance rewritten.
- Raises type-summary rebuilt (Richard): each window (+5d, +15d) now shows three aligned single-value columns — abs (median move from day-0 close), vs univ (median per-event excess over the equal-weight universe), hit (share positive, n complete) — under a grouped two-row header with rule separators; row labels are plain text (Equity/ATM/Convert/Preferred/Debt/Other) instead of small pills; footnote/eyebrow updated. Immediate read: converts are the only type positive vs the universe in both windows (+1.1/+2.4pts after −8.5% day-0); preferred's +17.9% abs at +15d is −5.0pts vs universe (beta, not recovery). Cache-bust v16.
- Raises tab trimmed (Richard): +30d and Excess+30d columns removed from both the aggregate and event tables — main view is now absolute day-0 / +5d / +15d only; the +30d path and per-window excess-vs-universe remain in each row's click-open detail; summary strip retargeted to +15d; eyebrow + footnote now state explicitly that table returns are absolute moves. Derivation unchanged (all windows still computed). Cache-bust v15.
- Raise-event backfill, FULL universe (5-agent EDGAR/wire sweep, 2025-06-26 → 2026-07-09): 101 events added → 106 total across all 21 names, every record source-cited (8-K/424B5/6-K/PR + acceptance timestamps for the ah flag) and machine-validated against the ledger before entry (0 hard failures; 2 flat-day-0 prints kept with the stale-print caveat — BTBT 7/15/25 backtest-era forward-fill, SLNH penny-flat weekend). Date discipline: launch wire not pricing/close (launch sizes recorded, upsized finals in terms — e.g. IREN 5/11 launch $2.0B→$3.0B close), converts flagged ah when launched post-close, ATMs at program announcement (cap as size), refis/refixes/JVs-without-disclosed-cash excluded (RIOT 4/21 refix, NBIS Avride, WULF Abernathy JV wire), ABTC pre-listing placement recorded on ABTC not HUT. Mix: 50 debt / 21 convert / 15 equity / 9 ATM / 6 pref / 5 other-ish; 4 pre-listing facts without returns (2 IPOs, ABTC ×2), 1 pending (FRMI $350M convert launched 7/9 intraday — awaiting first mark). First read of the hypothesis (medians, complete windows only): CONVERTS confirm it — day-0 −8.5%, then +2.7% by +15d and +2.5pts excess at +30d; EQUITY does not — −6.6% at +30d, 36% hit, −13.8pts excess; DEBT greeted positively (day-0 +1.4%) but lags the universe after (−11pts). Checks 4,373 — 0 FAIL (3 informative warns: SLNH/ABTC/BTBT ATM caps exceed market cap, verified real).
- New RAISES screen (spec §6, nine screens) — the capital-raise event study: sortable event table (company, announced, type pill, size, day-0 reaction, +5/+15/+30 trading-day returns, excess +30d) + aggregate table by instrument (median · hit rate · N, complete windows only) + summary strip, all derived at render time from the portfolio ledger (day 0 = first session on/after the announcement, `ah` rolls to the next session; windows anchor at the day-0 close; excess = minus the equal-weight bench over the same window; incomplete windows show italic return-so-far and never enter aggregates; pre-ledger events render dimmed as facts without returns). Row click → terms, purpose, size vs today's cap, per-window excess, source. Zero new storage — reads `portfolio-history.json` in the browser (Portfolio-tab lazy-load reused). Cache-bust v13→v14. Verified: NBIS 3/11 renders +16.1% / +5.9% / −9.0% / +40.3% / xs +15.3 (hand-checked vs ledger), all eight prior tabs unchanged, checks 0 FAIL.
- Raises registry live (spec §6c): `raises[]` seeded with 5 ledger-verified events — NBIS 3/11/26 same-day pair ($4.34B convert + $2.0B Nvidia prefunded-warrant placement, day-0 +16.1% confounded by the stake news), CRWV DDTL 4.0 $8.5B (3/31, day-0 +12.0%) and DDTL 5.0 $3.1B (5/18, day-0 −3.3%), IREN $3.65B IG GPU financing (6/1, day-0 +2.8%, +30d window still running). Held out: RIOT 4/21 loan refix (no new capital — raises[] records new-capital announcements only) and IREN's 5/14 convert (log date is the CLOSE; the launch date goes in with the backfill). Checks capital group +9 assertions per record (ISO date, future-date, kind enum, source, sizeM, ah type; warns: pre-ledger, duplicate, size>mcap) — 3,464 checks, 0 FAIL.
- Spec §6 → nine screens: RAISES added (the capital-raise event study — day-0 reaction + 5/15/30 trading-day forward returns, absolute and vs the equal-weight universe benchmark, derived at render time from the §6b ledger, never stored); new §6c defines the `raises[]` registry (dated fact records like leases — d/ah/kind/sizeM/terms/use/source, display-only, engine-inert); §5a reference-facts list gains `raises[]`. Docs only — registry, checks, screen and backfill follow.
- Coverage tab: Ann÷cap is now the main column — rightmost, bold, and the default sort (contracted-revenue yield on market cap); Backlog÷cap moved one slot left. Default order now CRWV 39% / APLD 29% / IREN 22% / WULF 17%.
- New COVERAGE screen (spec §6, now eight screens) — confirmed contracted value vs today's market cap, one row per name, every column sortable, row-click to the per-contract breakdown with terms. Kept live: market cap = reported (basic) `sharesReported` × live price (tracks the tape; NOT fully-diluted). Two display-only data fields added, engine inputs untouched: `sharesReported` per company (21, basic shares from stockanalysis/Yahoo, e.g. APLD 285.8M vs FD 380M) and `grossTotalM` on each effective lease (21, the GROSS headline contract value — e.g. WULF-Anthropic $19B, APLD $36.2B — distinct from the engine's NOI `noiPerMWyr`; compute contracts already carry `totalRevM`). Columns: market cap · contracted (gross) · gross-weighted avg term · annualized (Σ gross÷term, a term-average not a run-rate) · annual÷cap · backlog÷cap. Universe: $194.5B mktcap · $267.7B contracted · $37.8B/yr · backlog 138% of cap; leaders APLD 414% / WULF 267% / CRWV 218%. Checks add gross≥NOI-base-term and sharesReported≤FD tripwires (3,419 assertions, 0 FAIL). Sourced from the 21-agent market-cap/contract-book sweep + APLD per-lease term verification (all six APLD leases confirmed ~15yr base with renewal options on top, excluded).
- ORCL researched, built and HELD OUT of the universe (Richard's decision 2026-07-06): full owner-model entry constructed from the FY26 10-K (netDebt $105.3B incl. finance leases, FD 2,997M incl. mandatory-convert preferred, $638B RPO reconciled to OpenAI $300B + Meta ~$20B + $103B residual, 9.4GW leased-shell Stargate roll-up, legacyEV $210B, signedRate margin-normalized 13.3→8.0 for the $260B off-BS shell-lease structure) — 33 load-bearing facts adversarially verified (29 held exactly), board signal −42% ($144 → $83). Entry archived in git (commit bce68ba) for cheap revival; netDebt sanity bound reverted to 60000.
- FRMI refreshed from Q1 10-Q + July filings: price $8.60, shares 638→630 (filing-exact 629.84M), thesis re-anchored on the now-DATED binary (mgmt targets binding tenant ~mid-Aug; ≥400MW lease by ~Nov 10 or MUFG doubles amortization and can market the turbines; Keystone/TTU need a customer by Dec 31) — added as a watchItem; turbine book deepened (3×SGT6-5000F $277M paid + 6×SGT-800 + 7 TM2500 deferred to 2027; $785M equipment stack); proxy war logged (Neugebauer suspended 7/3 post-judge-recusal, bylaw fight alive; Rick Perry sold 864k @ $7.31; going-concern raised-then-alleviated via $156M Yorkville standby, up to +40M shares); equityDiscount 0.15 basis updated; verified stamps 2026-07-06. Board: FRMI $8.60 → $12.03 (+40%).
- Weekly sweep rewired to consume the source registry (Richard: sources are for USE in regular checks, not just validation): the `weekly-thesis-refresh` scheduled task now iterates `sources[]` from data.json each run — never a hardcoded list — with a dedicated commentary-sweep step (every commentary feed checked for new output on covered names; concrete claims verified against filings before entering; unverified claims capped at the source's provCeiling, soft state only; per-source digest in the weekly report) and per-fact registry-id citations. Adding a source = one `sources[]` entry; the next sweep adopts it automatically. WIKI "Keeping it current" documents the loop.
- Sources: five close-follower commentary feeds added (identified + adversarially verified): Agrippa Investments (Substack — IREN at extreme depth, disclosed IREN/NUAI long), Daniel Romero / Hyper Tech Invest (Substack — NBIS flagship plus CRWV/IREN/GLXY/APLD/RIOT comps, paywalled), Frans Bakker (X — top IREN retail account, filing forensics + weekly Space that has hosted co-CEO Dan Roberts), Stephen Zeiner (YouTube — KEEL/HIVE/SLNH/Fermi reaction commentary, ceiling rumored: no models/interviews), Springbok Finance (Substack — anonymous neocloud portfolio research, ceiling estimated). All typed commentary; per Richard: close followers of the clouds, not canonical sources.
- Preferred-source registry materialized (spec §9, first asked-for entry: McNallie Money): new top-level `sources[]` in data.json — market data (Finnhub, Coinbase), primary filings (EDGAR, company IR), proprietary research (SemiAnalysis; sell-side cross-check-only), curated commentary (McNallie Money YouTube — management interviews enter as estimated until filed, opinion is a lead not a fact), and the refuted list (BTC Miner Terminal, provCeiling none). Each source carries a provenance ceiling; Checks config group now validates the registry (ids, types, ceilings; commentary can never carry disclosed).
- Compute-contract sweep, FULL universe (Richard's flag: 'HIVE has one, and others' — confirmed; 13-agent sweep + adversarial verify). ADDED: HIVE — $220M/3yr Bell-AI-Fabric-for-Cohere sovereign contract (2,304 GB200s, signed 6/18/26, ~$70M ARR from go-live) + $30M/2yr live book → signedRate $14.5M/MW·yr (top-of-ladder GB200 print), contractedPct 35→10 (registry-honest: ~6MW signed vs 84 credited), thesis rewritten; WYFI — 8 confirmed cloud records (~$268M live committed: Paris IG $160M/5yr, $50.2M H100, $18.1M B200, Hyperbolic $17M, Boosteroid $18.3M cumulative, +2 small; the 2023 Initial-Customer $150M carried at ZERO — non-performing since Nov-25 payment dispute; DNA Fund REFUTED-terminated, excluded), distinct from its Nscale/Cerebras colo leases; HUT — Highrise AI $46M-inferred/5yr (1,096 Hoppers, fixed-fee + rev-share); APLD — legacy Sai/ChronoScale 'up to $180M' 2023 contract. REFUTED/NONE-CONFIRMED (explicit): BTDR (AI-cloud ARR $69M growing fast but ZERO named/per-contract disclosure — logged, excluded until one prints), SLNH (usage-based MSAs, wound down), MARA, CORZ, CIFR, RIOT, GLXY, CLSK, KEEL, NUAI, FRMI. Registry now 28 records; every name's compute-contract status is a verified fact, not a gap.
- Header: removed the 'Researched first-principles estimates…' notice badge.
- Leases tab: the Compute Contracts table brought to full parity with the landlord table — sortable columns (Owner · Counterparty · Gen · Base term · ~IT MW · $/MW·yr), expandable per-contract dossier (signed, term, status, annual run-rate, MW basis: COMPANY-DISCLOSED vs analyst inference, filing source, page link), owner summary strip (16 contracts · ~$148B book · signed rates CRWV $9.3~ / NBIS $11.5~ / IREN $10.1 disclosed) and the gen-ladder line (hopper ~$9.3 → blackwell $9.7-11.6 → VR 1H27 est. $13-16). Inferred MW figures filled from the research, all ~-flagged.
- OWNER COMPUTE-CONTRACT REGISTRY (Richard's thesis: GPU clouds re-sign up the generation ladder — CRWV undervalued; SemiAnalysis corroborates: consensus implies FLAT blended $/MW). New `contracts[]` per owner — dollars+term are the facts, MW flagged as inference (unlike colo leases, $/MW is never contractual): CRWV 9 records reconciling to the $99.4B 3/31/26 backlog (Microsoft $10B verifier-corrected, OpenAI $22.4B aggregate, Meta $35.2B aggregate incl. the $21B VR-era order form executed 3/31/26, Anthropic ~$6B bounded estimate, Jane Street $6B, $19.8B residual; NVIDIA's $6.3B backstop obligation on UNSOLD capacity through Apr 2032 noted — a contractual floor under the uncontracted book); NBIS $34.2B ≈ its $33.6B RPO (Meta-2 corrected to $12B firm of up-to-$27B); IREN $14.1B (the only owner with DISCLOSED MW: $9.70M and $11.33M/MW·yr prints). ENGINE: owner two-slice rate — signed slice @ company `signedRate` (CRWV 9.3 / NBIS 11.5 / IREN 10.1, basis-noted, checks-reconciled); lock reweighted to term ÷ capitalization-years (≈multiple) so ~30% of a 5-6yr book prices at the RE-SIGNING; unsigned + re-sign slices ride the new `gpuTrend` dial (default +8%/yr from the measured generation ladder — Hopper spot +38% off Oct-25 trough, Blackwell prints $9.7-11.3M, VR est. $13-16M with REVENUE FROM 1H2027 BY OWNER MANDATE; bull 15-20, bear ≤0) × per-company `genAccess`; landlord rents keep the separate rateTrend dial. Leases tab gains the Compute Contracts table (inference-flagged). Board: CRWV +36%→+60% ($163), NBIS +8%→+31% ($316), IREN +202%→+240% ($156). Watch-item: CRWV's Apr-Jun ~$8-9B debt wave not yet in netDebt (Feb-era $22.9B) — re-verify.
- Leases tab: all six columns sortable (click header to sort, again to flip — sites-table pattern; strings default ascending, numbers descending; active column carries the arrow). Default order remains annual NOI.
- Leases tab: tenant column de-cluttered (campus parentheticals were an artifact of pre-tab registry strings — campus has its own column; tenant cell now carries only the counterparty + credit info like 'Fluidstack · Google backstop'); column order → Lessor · Tenant · Campus · Base term · IT MW · NOI $/MW·yr; Beacon Point base-term value filled ($9.8B, from the lease PR).
- Leases tab simplified: six scan columns (Lessor · Tenant · Campus · IT MW · NOI $/MW·yr · Base term $); everything else — kind, signed vintage, term years, gross MW/IT ratio, annual NOI, value added, campus-leased runway, first rent, the filing source, and the company-page link — moved into an expandable per-row detail (sites-table pattern). Row click toggles; summary strip + print tape unchanged.
- Leases tab: swapped the %-of-EV out of the value column (model-derived, duplicated the gauge) for a CAMPUS LEASED column — this lease's IT MW ÷ the campus capacity the model credits (leased + graded forward rows), i.e. the expansion runway on land/power that already exist: Helios 25% of 2,126MW, River Bend 20% of 1,245, Beacon Point 50% of 702, PF1 36% of 700, Barber Lake/Black Pearl/PF3 100% (fully-let campuses). Value-added stays as the $ contribution. Campus grouping via site-name stems; denominator deliberately excludes marketing ambitions.
- New screen: LEASES tab — the registry rendered as the market print-tape. Per book: lessor, tenant + credit support (end-user/backstop, e.g. Fluidstack-for-Anthropic w/ Google), campus, kind pill (retrofit/conversion/build-to-spec), signed vintage, term, critical-IT MW (gross in parens where disclosed), term-avg NOI $/MW·yr, annual NOI $M, base-term $B, and the capitalized VALUE CONTRIBUTION ($ and % of company EV — connecting each lease to the target it drives). Header: 21 effective books · 4,546MW IT · $102B base-term · blended $1.62M NOI/MW vs the $1.76M forward anchor, plus median prints by kind (retrofit $1.02 / build-to-spec $1.51 / conversion $2.01) and by half-year vintage (rising from $1.20 in H1-24). Signed-not-effective (Tydal) dimmed + excluded from totals/engine. Row click → company page. Spec §6 updated; deliberately excluded: escalators/renewal schedules (DCF creep).
- HUT: OWNER MANDATE — River Bend ROFO assumed exercised (Fluidstack, up to 1,000MW IT under the 2.295GW Anthropic umbrella): tranche 1 400MW regraded rumored→ESTIMATED (Entergy power expansion still pending, so not disclosed), balance 600MW added rumored. Annotated in basis, developments log, and watchItems with an explicit falsifier (no Entergy/ROFO progress by mid-2027 → regrade down). HUT −20% → ~+4% (~$123) — the assumed exercise closes most of the street gap; thesis updated to 'roughly fair, paid-forward on the rest'.
- HUT undervaluation decomposition (Richard's challenge: street PT ~$120-140, price ~$118, our ~$84 — what do they know?). VERDICT: nothing hidden. Northland's own 15x-NOI floor computes $83.81 — our number to the dollar; KBW's $138 gets there by capitalizing MODELED/UNSIGNED River Bend + Beacon Point phases at 6.7% cap (same multiple as ours — the premium is unsigned leases credited as signed, which our facts-bind constitution refuses); VanEck's Sigel publicly argues the opposite corner (unleased at $2.1M/MW, pipeline at zero — LESS than we credit). Applied per our own scale-on-trajectory rule: Beacon Point ph2 300→350MW IT (1GW AEP interconnect executed), River Bend ROFO 245→400MW rumored (of 1,000 ROFO; no interconnect, HUT excludes it from its own pipeline); 7GW exclusivity/diligence NOT credited (verified attrition: the Aug-25 180MW TX project quietly vanished from the development bucket). HUT −29% → −20%. ALSO: ABTC 1-for-15 reverse split applied (shares 1,060M→70.9M, price →$7.90 — pre-empts a phantom-value break when the live feed serves post-split quotes) + treasury 7,200→8,000 BTC; ABTC fell ~22% since 6/30 (BTC weakness), so the market now marks HUT's stake ~14% BELOW our fundamentals look-through — the ABTC leg argues AGAINST the street on HUT, not for it.
- LEASE REGISTRY (Richard's mandate: values rooted in ACTUAL lease economics): new `leases[]` per landlord — one record per signed rate book ({counterparty, MW, term, TERM-AVERAGE NOI/MW, kind: retrofit/conversion/build-to-spec, effective, source}), 22 books pinned to filings across 9 landlords (15-agent research + adversarial verify). Engine v3 landlord path: SIGNED sites value at their actual contract NOI (facts — immune to anchor recalibration, full cap compression, no ramp); UNLEASED sites at the market anchor × region × NEW size factor (physical block <100MW ×0.9 / >300MW ×1.1, `physMW` for JV slices) × lease-up × trend — no compression without a lease. `mtm` retired (real rents replaced it); landlord `contractedPct` now registry-derived; new 'Lease registry' checks group (mapping reconciliation ±25%, NOI bounds, kinds, contracted%-vs-derived). Print tape: $0.60M (CORZ Austin retrofit w/ head-lease) → $2.06M (GLXY Helios). Board: GLXY +25→+60% (rich book was under-marked), WULF +20→+50%, APLD +53→+68%, CORZ +45→+27% (cheap 2024 book — loss-to-lease), NUAI ~+69% (physMW fix), RIOT +48→+53% (AMD retrofit cheap AND Corsicana forward rich — held simultaneously). CIFR third-campus watch-item resolved (Stingray = 70MW IT). Spec §5/§5a + WIKI synced.
- Calibration (Richard's call): base landlord NOI 1.2 → 1.6 — the global anchor moves to the MIDPOINT of the bullish case ($1.76M/MW·yr cheap-owned; conservative = WULF/Anthropic year-1 de-escalated ~$1.5M, bull = term-average ~$2.0M, which is the engine-consistent convention since contracted rent never escalates in the model). All landlords re-rate ~+33%: WULF −28%→−5%, APLD +7%→+53%, CIFR +22%→+72%, RIOT +13%→+48%, KEEL +144%→+226%, HUT −64%→−39%. WIKI benchmark line updated.
- WULF: Anthropic DIRECT 20-yr lease (8-K 7/6/26) — ~$19B / ~401MW critical IT at Hawesville KY 'Justified Data' (phased 2H27→early-2028, 2x5yr renewals, IG credit support reportedly Google; Century Aluminum's 6.8% Raylan minority already in seniorClaims). Simultaneously SOLD the 50.1% Abernathy JV stake to Fluidstack for ~$530M (~1.18x invested) — resolves the Abernathy over-credit watch-item; ~$500M receivable booked in legacy. Sites: +401MW disclosed Anthropic rows, −168MW Abernathy; contracted 38→45%; term 13→16yr. Economics verdict: implied YEAR-1 NOI ~$1.5-1.6M/MW (term-avg ~$2.0M incl. 2.5-3% escalators) — top of the market cluster (~$1.3M), at/above WULF's own prior Hawesville estimate, still ~35-40% below premium NoVA (DLR $2.51M). Validates the rising-rent trend (+3%/yr dial ≈ the signed escalators) and the lease-up conviction (empty smelter bought Feb-2026, 401MW leased within 5 months). Target $17.40→$22.21; new watch-item: KY permanent financing to term out the $500M MS bridge (→ committedDebt when issued).
- Portfolio: BTBT EXCLUDED from the paper book by mandate (Richard 2026-07-06: special case — holdco/SOTP discount vehicle; stays in the tracker for the HUT/WYFI look-throughs). New `exclude` mechanism in portfolio.json (ticker → basis, checks-enforced like every judgement input): excluded names are uninvestable for book AND benchmark — the position sells at market at the next mark, the benchmark rebuilds immediately (book and yardstick must share a universe), and the backtest preserves+honors exclusions across regenerations; spec §6b + WIKI updated. Portfolio tab: chart now plots cumulative % RETURN (both lines rebased to 0% at window start — the old axis was NAV index levels) with a 1D / 5D / 1M / 6M / 1Y range selector, plus an ABSOLUTE RETURNS table (portfolio vs equal-weight per window); excluded names badge "out by mandate" in holdings; assets cache-stamped ?v=4.
- Portfolio hardening pass 2 (a second 37-agent adversarial review of the pass-1 fixes themselves: 16 confirmed, 1 refuted). CRITICAL: the freeze carve-out never renormalized target weights over the tradable sleeve — one missed quote on a 58%-of-NAV name would have force-sold every healthy position into cash for a day; targets now renormalize so a freeze at unchanged prices produces zero drift and zero trades. ALSO: corp-action accept band narrowed ±40%→±12% (3:2/4:3/5:4 splits slipped through unadjusted — a silent −19% NAV error on an IREN 3:2), split lookback window now spans the actual ledger gap (was hardcoded 1mo), ledger price history back-adjusts on splits (raw cross-split ratios would have poisoned learning windows as ±50-900% phantom returns), Yahoo-unreachable is now distinct from Yahoo-disagrees (a real crash during a Yahoo outage freezes WITHOUT quarantine credit instead of blacking out the ledger on day 3), quarantine escalation now records the day's mark and commits BEFORE turning the run red (PORTFOLIO_ALERT plumbing), the benchmark monthly rebuild carries frozen names instead of liquidating halts at phantom prices and defers on trade-suspended days, freshness stamps only after the corp-action screen (quarantined names age into the stale route), suspect names drop out of the investable rows, learning cadence anchors to the last successful learn with era-gated attempts surfaced as ledger notes (first live learn ~day 64, not an accident of backtest length), stale opposition counters reset for names absent from a window, corrupt books abort rebalancing instead of clamping cash to zero, basis validation never rescales off the stale manual fallback price, genesis freshness seeds from actual final-day prints, attribution shows all names (the top-12 slice hid the biggest detractor), a failed background refresh no longer wipes a healthy Portfolio tab, the Checks↔portfolio fetch loop on load failure is broken, the px-vs-fundamentals tripwire tightened to ±35%, and all assets carry cache-busting version stamps (?v=3 — stale browser caches were serving old app builds). Genesis regenerated: NAV 785.7 vs equal-weight 264.9.
- Portfolio hardening (overnight adversarial review: 100 agents, 42 confirmed findings, 5 refuted; all criticals fixed). CRITICAL FIXES: (1) corporate-action guard — a >40% overnight move is now split-checked via Yahoo events (share counts adjust value-preservingly) or quarantined on source conflict (3 suspect days → red Action; a reverse split would previously have booked a permanent phantom NAV gain and phantom trades); (2) ABTC basis break — Yahoo's chart serves ABTC on a 15× different share basis than its own live quote; the backtest now BASIS-VALIDATES every series against a live quote and rescales (return-preserving; ABTC ×0.0667) — without this, Monday's first live mark would have printed a phantom −93% ABTC move into the benchmark forever; (3) learning rule — excess is now measured vs the universe MEDIAN (the arithmetic mean let one +798% moonshot mark +149% winners as 'market disagrees'), m shrinks only on two CONSECUTIVE opposed windows (overlapping 63d windows triple-counted single episodes), recovers only when vindicated, and an era gate keeps simulated-genesis views out of live λ/m; (4) BTC/ETH now stored per ledger record and forward-filled on Coinbase outage (was: silent reprice of ~$4B of treasuries at the static $60k fallback). ALSO: feed-death detection (dead quotes ≠ holiday — Action goes red + opens a GitHub issue), partial-tape days mark NAV but suspend trading, >10d-stale names freeze as uninvestable, removed-from-universe names freeze rather than sell at phantom prices, intraday workflow_dispatch refused before 16:05 ET, cost-aware sizing (cash never negative; was negative 189/256 days), tie-averaged Spearman, sub-$10 ledger prices keep 4dp (cents-rounding was a 1.5% error on NUAI), backtest refuses to overwrite live records without --force and fails loudly on missing tickers, git push rebase+retry in the Action, atomic file writes. Spec §6b synced to the implemented weights rule (gross = min(1, Σν⁺/0.75), cash = remainder — not cash-in-softmax). NEW: 'Portfolio ledger' checks group (NAV recompute from holdings, px-vs-fundamentals basis tripwire, date monotonicity, staleness, state bounds — 68 assertions) in checks.js + the Checks tab; Portfolio tab gains per-name ATTRIBUTION (NAV points, simulated vs live columns), operational-notes feed (splits, quarantines, freezes — surfaced, never silent), dials-moved warning on Target-now, auto-refresh, retry on failed load. Genesis regenerated: NAV 750.6 vs equal-weight 264.9 (ABTC investable at the correct basis; sane learning), zero negative-cash days.
- New screen: PORTFOLIO — the self-balancing paper book (spec §6b, new; §2/§6/§10 amended). Sizing rule: ν = λ × confidence × m × ln(target ÷ price); softmax at T=0.15 (concentrated by mandate, no position cap); confidence = (contracted floor + legacy) share of EV less open watch-items; gross exposure scales with total edge (cash grows as it thins); 2pt rebalance band, 5bps paper cost; long-only + cash (shorting reserved). Learning adapts SIZING only, never data.json: λ follows realized rank-IC every 21 trading days, per-name multipliers shrink where the market persistently disagrees (surfaced on the tab; `conviction: true` exempts). Valuation engine extracted to shared `engine.js` (browser + node, one math, as-of date overridable — portfolio targets roll forward daily). 1-yr simulated genesis, 256 trading days (Yahoo adjusted closes + Coinbase BTC/ETH): NAV 675 vs equal-weight benchmark 265 (base 100), max DD 43% vs 54%, peak single-name weight IREN 68% — labeled machinery-validation, NOT alpha (today's inputs contain the year); live state restarts neutral (λ 0.75, all m = 1). Daily mark: GitHub Action 22:35 UTC Mon–Fri → `portfolio-run.js` (Finnhub quotes, holiday/idempotency guards) commits `portfolio.json` + `portfolio-history.json` (the one sanctioned time-series ledger) and Render redeploys.
- APLD: capped calls CONFIRMED (indenture-level research, Nov-2024 8-K Ex 10.2) — $51.75M cost, strike $9.752 / cap $14.72, covering the full 46.1M convert shares; the stock tripled through the cap so the hedge is frozen at ~$229M intrinsic (offsets only ~7M shares at $33, and never reduces what converting holders receive). Added as a $229M legacy asset → target $39.57→$40.13 (+21%). Warrant audit: NO tranche (Macquarie/CoreWeave/Jane Street/MAM/STB/AI Bridge) carries any issuer redemption or buyback right; convert callable Dec-2027+ but a call only accelerates conversion (cash settlement = the sole avoidance, ~$1.5B at $33); Series E preferred (~2.5M sh) is the only cash-redeemable instrument. Full-issuance count 386M validates our ~380M FD (Northland's 285M = basic only).
- Calibration: base landlord NOI constant 1.1 → 1.2 ($/MW·yr), anchored to SIGNED hyperscaler NNN lease prints across five names (APLD $36B/15yr/1.41GW ⇒ $1.34M/MW; WULF Hawesville ~$1.4-1.6M; BTDR Tydal ~$1.1-1.4M; CLSK Sandersville ~$1.0-1.2M; KEEL Panther Creek ~$1.0-1.1M — cluster ~$1.3M vs our prior $1.21M cheap-owned anchor). Cheap-market owned now prices at $1.32M/MW, matching APLD's own signed economics. All landlords lift ~9-13%: APLD +6%→+20%, CIFR →+54%, KEEL →+144%, HUT −64%→−56%.
- Universe → 21: added NUAI (New Era Energy & Digital — single-project TCDC/Odessa landlord option; 650MW secured BTM gas, top-4 hyperscaler in advanced talks, hard Oct-8-2026 Macquarie lease deadline; sites scaled to NUAI's ~20% JV economics under the Apollo/Stream LOI; 20% governance discount → +12%) and KEEL (Keel Infrastructure — CORRECTION: our earlier batch research wrongly dropped the ticker as invalid; KEEL is the Bitfarms successor via 1:1 April-2026 redomiciliation, which pre-April databases masked. Three owned/zoned/power-secured campuses — Panther Creek 350MW PJM, Sharon 110MW, Moses Lake 18MW — ~$920M liquidity, both capped-call converts OTM (stay in net debt per convention), zero leases signed, three targeted YE26 → +124%). Filing-verified capital structures; verified stamps seeded.
- New screen: CHECKS tab — the data test suite runs LIVE in the browser on every page load (shared `checks-core.js`; identical code to the `node checks.js` CLI, 2,400+ assertions). Shows: verdict strip, the 8 check groups with what-each-guards + pass counts, all findings, a per-company ✓/⚠/✗ matrix (hover for messages), filing-verification ages per name (`verified.capital`/`verified.contracts` + `config.verifiedPricing`, colored ≤30d/31-60d/>60d — stamped by the weekly sweep on approval), and the open `watchItems` registry (WULF Abernathy 50.1% JV, CIFR 70-vs-150MW campus, APLD Macquarie draws, MARA AI-MW, CRWV supply-entrant tripwire). A pass/warn/fail badge sits on the tab on every page. Spec §6 → five screens; weekly sweep now stamps verification dates.
- Methodology census tier 2 applied — committed project debt & senior claims, verified to filings: HUT committedDebt $7.5B (River Bend $3.25B + Beacon Point $4.25B closed SPV bonds funding credited campuses) → −64%; CIFR $4,341M ($3,531M escrowed construction SSN proceeds + $810M Stingray closed 6/15/26) + $26M Colchis NCI → +37%; APLD $3,740M (ComputeCo-2/-3 closed post-quarter, funded paper only) + $1,100M seniorClaims (Macquarie preferred ~$970M accreted + ~$130M JV NCI) → +10%; WULF committedDebt $0 (the $3.2B Lake Mariner bond is ALREADY inside its netDebt — escrowed proceeds are the netted cash) + $101M Hawesville NCI; Abernathy 50.1%-JV over-credit flagged for the weekly sweep. Financing is now charged symmetrically with the credit across the board.
- Methodology census applied (tiers 1+3 of 3; 34-agent adversarial review verdict: 'fit for purpose after repairs'). CONSISTENCY FIXES: contract premium & cap compression are now site-aware (rumored MW earns neither — removes ~$19-20B of unearned EV from CRWV/NBIS); ramp applies only to the uncontracted share (take-or-pay leases bill from commencement — un-flips CORZ); landlord cap floored at 6.5% (the DLR print); WYFI rerouted owner→landlord (~97% colo by economics; GPU cloud → legacyEV) + its ITM convert if-converted (shares 41.6→50.5M, netDebt −63) — flips WYFI to overvalued and collapses BTBT +204%→~+29%; BTBT stake diluted-through (pct 0.577) and controlling stakes use pre-discount equity. NEW DIALS (the silent bets made visible): 'Lease-up / spot realization' (base 1.0 — the deliberate scarcity conviction that energized capacity gets rented; 0.42 = consensus spread) and 'Rumored-pipeline credit' (stress the far pipeline without editing data); trend capped at disc−2 so the time axis can't invert; 7× multiple derivation + margin-as-steady-state-EBITDA documented in WIKI. New fields committedDebt/seniorClaims wired into equity (basis required, checks.js enforces); figures for HUT/CIFR/WULF/APLD pending filing verification (tier 2).
- Audit infrastructure: added `checks.js` — a deterministic data test suite (schema, site schedules/phasing, provenance consistency, capital-structure sanity, basis notes on judgement inputs, stake integrity, freshness); runs in seconds, exits non-zero on FAIL; wired into the weekly scheduled sweep (which also verifies FD shares/issuance/contracts/pricing against filings). First run caught 3 FAILs (undocumented equityDiscounts on SLNH/HUT/FRMI — bases added) + 24 legitimate watch-items. Synced stale spec §3/§4/§6 (removed slide-out panel, score, renewalProb, cost-of-debt-floor references); removed dead score code/CSS from app.js/styles.css; WIKI updated. [skip render]
- Added WIKI.md — a terse, present-tense, always-current plain-English reference (formula, dashboard gauge, dials, field glossary, conventions, how to keep it updated, known limitations). Companion to the binding spec.
- Dashboard graphics redesign: replaced the EV-composition stack + split bar + relative-strength score with a single per-row VALUE GAUGE — bar = our target value (segmented contracted-floor / expected-pipeline / legacy-treasury), a dark line = today's market price, and the shaded space between = upside (green) or overvalued (red), scaled per row. One visual now shows the price-vs-value gap, its direction, AND the quality of the value backing it (solid = contracted, hatched = speculative, gold = legacy/stake) — and it works for holdcos. Dropped the opaque relative-strength score from the dashboard and full page.
- Capital-structure overhaul (audited to 10-Q/10-K across the universe, 2026-06-30, prompted by a model-vs-BTC-Miner-Terminal audit + the Northland DLR/Blackstone note): corrected fully-diluted shares (if-converted/treasury — deep-ITM converts + RSUs + penny/ITM warrants, with each counted convert's principal removed from net debt) and net debt for 17 names. Biggest fixes: HUT net debt $7.5B→$85M (the $7.5B was a forward/undrawn SPV project-debt placeholder, not parent debt) → −67%→−27%; CORZ +63%→−2% (+138M shares + post-May refi debt); CIFR +95%→+42%, APLD +73%→+34%, BTDR +79%→+47%, CLSK +37%→+22% (missing convert/RSU dilution); NBIS → net cash −$9.3B if-converted (−3%); WULF −20%→−36%. We were too low on shares for 13/17 names. Spec §5a now codifies the FD-share / net-debt discipline (project/SPV debt netted only to the extent drawn; escrowed construction cash credited).
- Dashboard: the per-ticker thesis is now COLLAPSED by default behind a "▸ why under / over-valued" toggle on each row (expands inline on click, doesn't trigger navigation) — declutters the comparison view. Also fixed the thesis text colour (was referencing an undefined CSS var).
- Dashboard: per-ticker THESIS line — a 2-3 sentence, high-signal read on why each name is under/over-valued (what's driving the model-vs-market gap), shown under each comparison row. Stored as a `thesis` field per company; meant to be refreshed regularly as prices and contracted MW move (a recurring review step).
- FRMI (Fermi / Project Matador): regraded to credit the SITE SIZE — ~2 GW firm secured power (200MW grid + 1GW gas Ph1 estimated + 800MW gas Ph2 rumored), backed by the approved ~6GW TCEQ air permit + Energy Transfer firm gas + 1.5-2.2GW turbines ordered. Credited as raw-power optionality (pre-tenant, contracted 0%), NOT built/contracted; 11-17GW nuclear/SMR ambition credited at ~zero. Target −60% → −16%: the firm 2GW roughly supports the price, the premium is the ambition + OpenAI-anchor optionality (the re-rating catalyst).
- Engine: SOTP / holdco support — a company can hold a live look-through `stake` ({tk,pct}) in another tracked name (pct × its modeled equity, auto-updating) and an `eth` treasury marked to a live ETH price (Coinbase). New `holdco` model (no sites; value = stakes + treasuries + legacy − net debt). Facts panel shows the SOTP breakdown and flags when a stake alone exceeds the whole company's market cap.
- Universe → 19: added ABTC (American Bitcoin — BTC treasury/miner, tracked so HUT marks its stake live), CLSK (CleanSpark — real but 100% pre-contract AI/HPC landlord pivot; Meta 'frontrunner', ~1.1GW TX/GA, 13,470 BTC), BTBT (Bit Digital — modeled as a holdco: 70% WhiteFiber/WYFI look-through + 158k ETH; trades at a deep SOTP discount, stake alone ~1.5x its market cap → +219%).
- HUT: now tracks American Bitcoin (ABTC) as a live ~55.3% look-through stake, REPLACING the old generic $600M legacy (which the audit showed was already standing in for ABTC/mining — avoids double-count). Net slightly lower (−67%); the $7.5B nonrecourse campus debt still dominates.
- Universe expanded 5 → 16 names (multi-agent first-principles research + adversarial verify). Added GLXY, HUT, CIFR, MARA, CORZ, WULF, APLD, SLNH, FRMI, WYFI, HIVE. DROPPED FUFU (pure BTC miner, zero AI — confirmed), CLSK (near-pure miner), KEEL (not a valid AI ticker); HELD BTBT (its AI lives in the separately-listed WhiteFiber/WYFI stake — adding both double-counts; BTBT itself is mostly an ETH treasury). BTC counts reconciled to dedicated research (WULF→0 post-liquidation, MARA 36.3k, CIFR 1.5k, CORZ 547). SLNH cut to realistic ~280MW AI + $750M dilution (was an +861% artifact). HUT $7.5B confirmed nonrecourse SPV project debt (parent recourse ~$0) — correctly netted vs the gross-credited campuses → thin levered equity (−66%). MARA (AI-MW overstated, ~200MW real) and FRMI (pre-revenue power-site optionality, richly priced) kept as flagged signals.
- Cleanup / consolidation pass (cold-read traceability audit): the one binding formula documented in spec §5/§5a; removed dead/inert fields (`contractRate` constant, `renewalProb`, per-name `costOfDebt` — discount is now the single `disc` dial); collapsed to ONE legacy convention (`legacyEV` = non-BTC residual, `btc` marked live; dropped `legacyExBtc`); panel shows `mtm` for landlords / term for owners only, relabels `leaseQ` as ranking-only, and shows a sourced `basis` note on every judgement input (plannedRaise, equityDiscount, non-Proven tier). Fixed the value-bridge caption to divide by FUNDED shares (was raw shares). No target moved — pure decluttering.
- Engine: funding-dilution mechanic — per-company `plannedRaise` (realistic ATM/equity issuance, research-grounded) × a global `dilutionStress` dial; target = built-out equity ÷ funded shares (shares + raise ÷ live price). Replaces the earlier full-capex approach (which double-charged capital the multiple already embeds). Raises: CRWV $15B, NBIS $10B, IREN $8B, BTDR $0.575B, RIOT $0.25B. Board: CRWV +56%, NBIS +16%, RIOT +13%, BTDR +75%, IREN +217%.
- Engine: geography rate factor — regions carry a `rateMul` (US 1.0; EU 0.70, AU 0.70) so non-US AI capacity isn't valued at US GPU rates (Spain/Australia).
- IREN: rebuilt to its real ~5.8 GW global pipeline (was 2.2 GW) — Childress (→750MW incl. mining→AI conversion), Sweetwater 1+2 (2 GW), Kiowa OK (1.6 GW), Nostrum Spain (490MW, EU, rumored), Bundey S. Australia (800MW, AU); BC corrected (Prince George AI, Mackenzie mining→AI, Canal Flats → mining/legacy). Shares 357→373M for the all-stock Mirantis + part-stock Nostrum acquisitions; Mirantis software credited ~$500M in legacy. Near=firm/far=rumored grading. Target ~$146 (+217%, post-dilution).
- Live BTC price: legacy marks each holder's BTC treasury to a live BTC spot (Coinbase, hourly + manual ↻): legacyOf = btc × live price + `legacyEV` residual. RIOT 15,680 BTC; BTDR liquidated (no btc); IREN 0. BTC spot in header note + per-name treasury line.
- RIOT: re-rated to IG (cap 9.0% → 7.75%) — credits the scarcity / irreplaceability of Corsicana's ~1 GW powered, Tier-3-built campus (crown-jewel; fast time-to-online once leased). NOI kept as-is (AMD is a retrofit, not forward economics); Corsicana keeps full bull credit. Target ~$32 (+16%).
- BTDR: governance / control discount (15% off equity) via new per-company `equityDiscount` field + a "Gov. disc" step in the value-bridge waterfall. Reflects Jihan Wu ~75% voting control, foreign-private-issuer, controlled-company status, index exclusion. Target $37.53 → $31.90 (+101%).
- Engine: landlords now ride the GPU rate trend too (symmetric with owners) + base trend tuned +6% → +3%. Landlord NOI/rent escalates by vintage — contracted rent locked at today's level, uncontracted/future capacity earns the prevailing rent at its energization year (rising GPU $/MW → rising rents). Fixes the asymmetry where only owners re-rated on the trend, which had left RIOT frozen as the lone negative. At +3%: RIOT $26.37 → $28.42 (−5% → +2%; Corsicana's 2029/2031 buildings now earn rising rent), BTDR $35.89 → $37.53. Per-site NOI math shows today → vintage rent.
- Engine: vintage-rate model + "GPU rate trend / yr" dial (base +6%/yr, bull). $/MW·yr is now priced at the rate prevailing when capacity is contracted/energized, and that rate trends over time. The contracted share is locked at today's rate; the uncontracted share floats up to the future prevailing rate at its energization vintage — so unsold owner capacity is the upside in a rising-rate world, not a spot-haircut markdown. Replaces the owner spot "realize" (renewal × mtm) haircut; execution/lease-up risk now lives in provenance. Net relationship: the real discount on future compute = discount − trend. At +6%: IREN $85→$125, NBIS $307→$361, CRWV $182→$198. Landlords (RIOT/BTDR) are GPU-rate-insensitive (cap-rate driven) so they don't ride the trend — a known asymmetry to revisit. Per-site math and commercial facts now show the vintage rate + trend.
- Dashboard: hide the tier pill when a name is at the base "Proven" tier (only show it once re-rated to IG/IG-REIT) — removes redundant "PROVEN" labels; full page still shows the tier.
- IREN (IREN Ltd / Iris Energy): added as 5th name (multi-agent workflow + adversarial verification). Owner / GPU cloud (owns the GPUs; Microsoft ~$9.7B + NVIDIA ~$3.4B are cloud contracts, not leases). Contracted 20% (Microsoft a non-representative anchor per mgmt → spot-tilted); net debt ~$3.85B current (incl. Jun-1 $3.65B IG GPU financing); legacyEV $500M (mining fleet; BTC treasury = $0, sells daily); 13 phased AI sites / 2.2 GW owned cheap power (existence-credited). Target ~$85 (+85%) — but PRE-funding-dilution: a ~$20B+ buildout funded largely by the $6B ATM + converts will dilute the per-share number heavily (the dominant caveat).
- Navigation: hash routing — each company's full valuation is its own URL (#TICKER, deep-linkable/shareable); #sites and #sites=TICKER for the sites tab. Removed the slide-out panel — clicking a dashboard row goes straight to the full page. Back button / Esc return to the dashboard.
- All-sites page: filterable by ticker (the full page's "where the value comes from" links to #sites=TICKER); each row is expandable to its per-site math (moved off the full page). Removed dead panel/per-site-bar code.
- Build-out chart: hover any column for a tooltip listing the sites online by that year, grouped under disclosed / estimated / rumored with MW each.
- Full-page valuation redesign: a build-out-over-time chart (cumulative capacity/value by energization year, stacked by provenance, MW ↔ $ toggle) as the headline, plus a value-bridge waterfall (Sites → Legacy → Net debt → Equity → target); per-site expandable bars kept as the drill-down. Hand-rolled SVG, dependency-free.
- Live prices: Price column added left of Target on the comparison dashboard, fed from Finnhub (`config.js` holds the token), refreshed hourly plus a manual "↻ refresh" button. Live price drives the upside calc (target ÷ live price − 1) and the panel/full-page price; falls back to the manual `data.json` price if no token or the feed is unavailable. A status note shows the last refresh time.
- Header notice updated from "Illustrative placeholder data" to a present-tense, first-principles disclaimer (all four names now researched).
- Provenance discipline fix (landlords): provenance = existence (land/power/permitting/construction), NOT leasing. Owned/approved/scheduled capacity is `disclosed` even when unleased; leasing risk lives in `contracted %`. Re-tagged RIOT (all owned/approved → disclosed) and BTDR (owned → disclosed; Clarington stays rumored, litigation-stalled). RIOT $10.95 → $26.37 (−7%); BTDR $19.76 → $35.89 (+101%, cheapest per owned-MW of the landlord pair).
- RIOT: real researched data replaces placeholders (multi-agent workflow + adversarial verification). Landlord (colocation) — sole signed AI lease is AMD 50 MW at Rockdale (10-yr); Corsicana ~1 GW built on spec, zero tenants. Net debt +$636M (0.75% converts + BTC-backed loan; BTC excluded); legacyEV $1.36B (15.7k BTC treasury + FCF-negative 42.5 EH/s mining fleet + ESS Metron, BTC-spot-sensitive); contracted 5%; 7 phased AI sites / 1.04 GW, ~all estimated/rumored. Target $10.95 (−62% at $28.50) — first-principles read flags the +90%-YTD AI-pivot run as pricing far more optionality than is contracted.
- Engine: investability-tier / re-rating axis — base case for every name is Proven (the cap/multiple dials); each company can be taken UP to IG or IG/REIT grade (landlord cap-rate compression, owner multiple expansion), baked into the target. Never marked below Proven — provenance + contracted % already carry execution/lease-up risk (no double-count). Tier shown as a pill (board + one-pager), in commercial facts, and in the per-site math. All four names default Proven. Sell-side price targets are not a benchmark — valuation is first-principles from assets/income/re-rating.
- BTDR (Bitdeer): added as 4th name (multi-agent workflow + adversarial verification). Landlord model (AI colocation pivot, RIOT archetype) — flagship Tydal colo lease signed 2026-06-29 but not yet effective. Net debt +$1.66B (cheap converts); legacyEV $1.34B (self-mining + SEALMINER ASIC + BTC, low-confidence/BTC-cycle-fragile); contracted 5% (AI pivot near-zero firm); 11 phased AI sites / 1.4 GW, ~all estimated/rumored. Target $19.76 (+11% at $17.88). legacyEV built bottom-up from assets (mining fleet + ASIC business + BTC). Note: landlord path credits unleased capacity at market NOI — a known limitation to refine (lease-up haircut).
- NBIS: real researched data replaces placeholders (multi-agent workflow + adversarial verification). Net cash −$848M (prepayment-funded, convertibles); legacyEV $5.8B from non-core stakes (ClickHouse ~$4.2B, Avride, Toloka, TripleTen); 80% contracted / 4yr (hyperscaler backlog ~92% Microsoft+Meta, with high-rev-per-MW enterprise tail); 19 phased sites / 6.5 GW — scale sized on growth trajectory (Goldman cross-check $286) not headline ambition, capped 2031. Target ~$307 (+28%).
- Spec: synced §3/§4/§6a to the built model — ramp dial, site-aware contracted/spot + floor/upside split, first-power dates + rollout decomposition, expandable per-site valuation.
- CRWV: full rollout-schedule decomposition (14 → 20 site rows) — campuses phase in real announced blocks (Helios Ph1-3, Ellendale 1-2, Core Scientific Denton/Muskogee/Dalton, Lancaster A-B, Other-contracted by year), speculative pipeline spread to 2031 at ~fleet build rate. Build rates evidence-checked (Ellendale ~150 MW/yr, Helios ~300 MW/yr, fleet ~1-2 GW/yr; transformer/interconnect lead-times are the ceiling); ~75% of contracted book online by end-2027 (matches mgmt guidance). Ramp dial base 9 → 12 mo (per operator: ~9-12mo for a 50 MW hall to revenue). Target $182.
- Global dial: "Revenue ramp to steady-state (months)" — delays each site's value by N months in the time-discount, modeling commissioning / GPU fill / contract ramp after first power, without per-site phasing. Base case 9mo (CRWV target $216 → $198). Shown in each site's time-discount line.
- Full-page valuation: each per-site bar in "where the value comes from" expands to a live step-by-step breakdown (rate → margin → multiple → value/MW → gross → haircut → time-discount → site value, with contracted/expected split). Model-aware (owner vs landlord); recomputes with the dials. Panel sites stay a concise summary.
- Engine: site-aware contracted/spot split — uncontracted (rumored) pipeline earns the spot rate and moves with the GPU-rate dial; disclosed/estimated capacity carries the contracted book. New "contracted floor vs expected upside" split shown on the comparison bar and in each company's valuation build. Implements spec §4 (contracted book insulated, spot book moves).
- CRWV: real researched data replaces placeholders — 14 sites (full contracted book + ramped path to 8 GW by 2030), capital stack (~$22.9B net debt, 545M shares, $102), and sourced narrative. Target ~$218 at base case; ~21% of value rests on rumored capacity.
- Phase 1 app: four screens (comparison, sites, company panel + full page, assumptions dials), valuation engine ported from the prototype, all data loaded from `data.json` at runtime. Three names: CRWV, NBIS, RIOT.
- Scaffold Phase 1: repo, `CLAUDE.md`, spec and prototype copied in.
